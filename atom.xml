<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[]]></title>
  <link href="http://blog.abuiles.com/atom.xml" rel="self"/>
  <link href="http://blog.abuiles.com/"/>
  <updated>2014-02-21T10:15:45-03:00</updated>
  <id>http://blog.abuiles.com/</id>
  <author>
    <name><![CDATA[Adolfo Builes]]></name>
    <email><![CDATA[builes.adolfo@gmail.com]]></email>
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[Router, Routes and Templates]]></title>
    <link href="http://blog.abuiles.com/blog/2014/02/21/router-routes-and-templates/"/>
    <updated>2014-02-21T10:01:26-03:00</updated>
    <id>http://blog.abuiles.com/blog/2014/02/21/router-routes-and-templates</id>
    <content type="html"><![CDATA[<p><strong>This post is part of my series about <a href="http://blog.abuiles.com/ember-js-with-ruby-on-rails">learning Ember.js with Ruby on Rails</a>.</strong></p>

<p>We have now the basic skeleton to <a href="http://blog.abuiles.com/blog/2014/02/07/building-an-app-for-my-mom-with-ember-appkit-rails/">start building an app for my
mom</a>,
today we are going to talk about 3 important concepts in Ember.js: The
<code>Router</code>, <code>Routes</code> and <code>Templates</code>.</p>

<p>Grab the application from where we left last:</p>

<pre><code>  $ git clone git@github.com:abuiles/ember-rails-week-1.git
  $ git reset --hard week-1
  $ bundle install
  $ rails s
</code></pre>

<p>Go to <code>http://localhost:3000/</code> and you should see &ldquo;Welcome to Ember on Rails!&rdquo;.</p>

<h2>Creating the Application Template.</h2>

<p>Ember&rsquo;s equivalent of Ruby on Rails Application layout is called application too.</p>

<p>Create the file <code>app/templates/application.hbs</code> and put the following content there.</p>

<figure class='code'><figcaption><span></span></figcaption><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
<span class='line-number'>2</span>
</pre></td><td class='code'><pre><code class='html'><span class='line'><span class="nt">&lt;h1&gt;</span>Hi Ember.js<span class="nt">&lt;/h1&gt;</span>
</span><span class='line'>
</span></code></pre></td></tr></table></div></figure>


<p>Refresh your browser and you will see <code>Hi Ember.js</code>.</p>

<p>{{outlet}} is similar to <code>yield</code> on the application
template in Ruby on Rails <strong>but is not the same, there is also the
word <code>yield</code> on Ember.js we&rsquo;ll cover later</strong></p>

<p>Let&rsquo;s add something into the outlet, create the file
<code>app/templates/index.hbs</code> with the following</p>

<figure class='code'><figcaption><span></span></figcaption><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
</pre></td><td class='code'><pre><code class='html'><span class='line'><span class="nt">&lt;h1&gt;</span>I&#39;m the Index!<span class="nt">&lt;/h1&gt;</span>
</span></code></pre></td></tr></table></div></figure>


<p>Refresh your browser and you will see &ldquo;Hi Ember.js&rdquo; follow by &ldquo;I&rsquo;m the Index&rdquo;.</p>

<h2>Under the hood.</h2>

<p>To understand why the previous stuff worked without requiring us to
add any extra code we&rsquo;ll have to talk about the concept of <code>Routes</code>.</p>

<p>In Ember.js there is a <code>Router</code> and many <code>Routes</code>.</p>

<p><code>Router</code> is where we specify the entry points for our application,
this is similar to <code>routes.rb</code>.</p>

<p>In Rails <code>router.rb</code> every entry matches to an <code>action</code> in a
controller, this is not the case in Ember.js, we mentioned previously
that a <code>controller</code> in Ember acts more as decorators for our views.</p>

<p>In Ember every entry in the <code>Router</code> matches to a <code>Route</code>, if we have
in the <code>router.es6</code> an entry like:</p>

<figure class='code'><figcaption><span></span></figcaption><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
</pre></td><td class='code'><pre><code class='javascript'><span class='line'><span class="k">this</span><span class="p">.</span><span class="nx">route</span><span class="p">(</span><span class="s2">&quot;about&quot;</span><span class="p">,</span> <span class="p">{</span> <span class="nx">path</span><span class="o">:</span> <span class="s2">&quot;/about&quot;</span> <span class="p">});</span>
</span></code></pre></td></tr></table></div></figure>


<p>Then it will look for <code>app/routes/about.es6</code> and the template <code>app/templates/about.hbs</code>.</p>

<p>If Ember doesn&rsquo;t find the <code>route</code> it will create one automatically and
if we need to do any kind of processing before showing our template
like fetch data from a server or render a different template, then we
can do that on the <code>route</code> file.</p>

<p>With this in mind, the reason why putting <code>index.hbs</code> works without
doing anything extra is that Ember.js creates by default an
<code>IndexRoute</code> which matches to <code>/</code>.</p>

<p>When we visit <code>/</code> it tries to find the <code>app/routes/index.es6</code> if is not there it
create one by default and then renders the template under <code>templates/index.hbs</code>.</p>

<h2>A first Route</h2>

<p>Let&rsquo;s create a simple route for <code>index</code>, put the following under <code>/app/routes/index.es6</code>.</p>

<figure class='code'><figcaption><span></span></figcaption><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
<span class='line-number'>2</span>
<span class='line-number'>3</span>
<span class='line-number'>4</span>
<span class='line-number'>5</span>
</pre></td><td class='code'><pre><code class='javascript'><span class='line'><span class="kr">export</span> <span class="k">default</span> <span class="nx">Ember</span><span class="p">.</span><span class="nx">Route</span><span class="p">.</span><span class="nx">extend</span><span class="p">({</span>
</span><span class='line'>  <span class="nx">model</span><span class="o">:</span> <span class="kd">function</span><span class="p">()</span> <span class="p">{</span>
</span><span class='line'>    <span class="k">return</span> <span class="p">{</span> <span class="nx">date</span><span class="o">:</span> <span class="nb">Date</span><span class="p">()</span> <span class="p">};</span>
</span><span class='line'>  <span class="p">}</span>
</span><span class='line'><span class="p">});</span>
</span></code></pre></td></tr></table></div></figure>


<p>Replace the <code>index</code> template with</p>

<figure class='code'><figcaption><span></span></figcaption><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
</pre></td><td class='code'><pre><code class='html'><span class='line'><span class="nt">&lt;h1&gt;</span>I&#39;m the Index! <span class="nt">&lt;/h1&gt;</span>
</span></code></pre></td></tr></table></div></figure>


<p>After refreshing you will see <code>I'm the Index</code> follow by a date, we
just write our first route, bootstrapped the required stuff for our
template and then rendered it into the template!</p>

<h2>Task</h2>

<p>We want to have an <code>/about</code> page, add the necessary entry to the
<code>Router</code> and template.</p>

<h2>Resources</h2>

<p>I suggest going to the official Ember.js guides and check the section for Routing and Templates.</p>

<ul>
<li><a href="http://emberjs.com/guides/routing/">Routing guide</a></li>
<li><a href="http://emberjs.com/guides/templates/the-application-template">Templates guide</a></li>
</ul>


<h4>Want to learn more about  Ember.js with Ruby on Rails? Subscribe</h4>

<p><form action="http://emberenos.us7.list-manage.com/subscribe/post?u=103dd05cb2005f7b5485df96d&amp;id=25604cee1d" method="post" id="mc-embedded-subscribe-form" name="mc-embedded-subscribe-form" class="validate" target="_blank" novalidate><label for="mce-EMAIL">Subscribe.</label><input type="email" value="" name="EMAIL" class="email" id="mce-EMAIL" placeholder="email address" required><!-- real people should not fill this in and expect good things - do not remove this or risk form bot signups--> <div style="position: absolute; left: -5000px;"><input type="text" name="b_103dd05cb2005f7b5485df96d_25604cee1d" value=""></div><input type="submit" value="Subscribe" name="subscribe" id="mc-embedded-subscribe" class="button"></div></form></p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Building an App for My Mom With Ember Appkit Rails.]]></title>
    <link href="http://blog.abuiles.com/blog/2014/02/07/building-an-app-for-my-mom-with-ember-appkit-rails/"/>
    <updated>2014-02-07T10:56:18-02:00</updated>
    <id>http://blog.abuiles.com/blog/2014/02/07/building-an-app-for-my-mom-with-ember-appkit-rails</id>
    <content type="html"><![CDATA[<p><strong>This post is part of my series about <a href="http://blog.abuiles.com/ember-js-with-ruby-on-rails">learning Ember.js with Ruby on Rails</a>.</strong></p>

<p>In previous post we explored what is
<a href="http://blog.abuiles.com/blog/2014/01/27/ruby-on-rails-mvc-is-not-the-same-as-ember-dot-js-mvc/">Ember.js MVC</a>
and then did a
<a href="http://blog.abuiles.com/blog/2014/01/31/getting-started-with-ember-and-ruby-on-rails-using-ember-appkit-rails/">high level overview of Ember Appkit Rails</a>,
in this post we&rsquo;ll start to work on a project which we will use as
reference for upcoming posts.</p>

<h2>The Problem</h2>

<p>My mother lives in a small town in Colombia where she owns a store,
access to credit cards in Colombia is really really hard, so stores
and people in small towns tend to buy and sell on credit, trusting
that people will come back and pay them (crazy, isn&rsquo;t?).</p>

<p>During my last visit I was really shocked when I saw that she was
keeping all her records on physical invoices in under an A-Z folder.</p>

<p>If <code>John Smith</code> takes a t-shirt on credit, they would create an
invoice with his name, add a description (e.g. t-shirt) with the price
<code>$25</code> and then throw that into the A-Z in the section for surnames
starting with <code>S</code>.</p>

<p>Later John returns to the store and makes a partial payment (e.g.
<code>$10</code>) or pays the whole thing.</p>

<p>This approach is horrible, insecure, my mother doesn&rsquo;t know how much
is people really owing her, she can&rsquo;t have insights, etc.</p>

<p>Let&rsquo;s then  build an App for my mother where she can keep her records, the app name will be <code>facturas</code> which means <code>invoices</code> in Spanish.</p>

<h2>Starting the project.</h2>

<p>For this project we will use <code>Ruby on Rails 4.1.0.beta1</code> and <code>Ember.js 1.4.0-beta</code>.</p>

<p>Install Ruby on Rails <code>gem install rails --pre</code>, once it&rsquo;s completed
run <code>rails new facturas</code> and we should have the basic structure for
our project.</p>

<p>Let&rsquo;s keep everything under <code>git</code> so we can keep track of changes.</p>

<pre><code>  $ git init
  $ git add .
  $ git commit -m 'Getting started!.'
</code></pre>

<p>I use <a href="https://github.com/github/hub">hub</a> so I will just push this to a new repo on GitHub.</p>

<pre><code>  $ hub create ember-rails-week-1
    Updating origin
    created repository: abuiles/ember-rails-week-1
  $ git push origin master
</code></pre>

<p>Install <code>Ember Appkit Rails</code>, since I want to use a new feature we
have been working on to include testing out of the box, we won&rsquo;t
install from rubygems but directly from GitHub.</p>

<p>Add the following to you Gemfile:</p>

<pre><code>gem 'ember-appkit-rails', git: 'https://github.com/dockyard/ember-appkit-rails.git'
</code></pre>

<p>Run <code>bundle install</code>.</p>

<p>Now we can bootstrap our Rails project with Ember Appkit Rails.</p>

<pre><code>  $ rails g ember:bootstrap
</code></pre>

<p>For now just focus on this two files:</p>

<pre><code>  create  config/router.es6
  create  config/application.js
</code></pre>

<p><code>router.es6</code> would be the main entry point for our Ember App, it&rsquo;s
something similar to <code>routes.rb</code>, it will help us defined how users
interact with our application. We will talk more about it later but if
you can&rsquo;t wait, check the awesome <a href="http://emberjs.com/guides/routing/">routing
guide</a> on Ember.js.</p>

<p><code>application.js</code> is the replacement for <code>app/assets/javascript/application.js</code>.</p>

<p>Remove your <code>app/assets/javascripts</code> since it will be ignore by EAKR <code>rm -rf app/assets/javascripts</code>.</p>

<p>Start your rails server <code>rails s</code> and go to <code>http://localhost:3000</code>, you will see <code>"Welcome to Ember!"</code>.</p>

<p>We are ready to start! In the next post we will create our first
model, talk a bit about testing and keep our journey of mastery of
Ember.js with Ruby on Rails.</p>

<h3>Can&rsquo;t wait?</h3>

<ol>
<li><p>Create an &lsquo;Ember&rsquo; application template</p>

<pre><code>$ echo  &gt; app/templates/application.hbs
</code></pre></li>
<li><p>In <code>config/routes.rb</code> uncomment the last line <code>get '*foo', :to =&gt; 'landing#index'</code>. It should look like <a href="https://github.com/abuiles/ember-rails-week-1/blob/master/config/routes.rb#L64">https://github.com/abuiles/ember-rails-week-1/blob/master/config/routes.rb#L64</a></p></li>
<li>In route.es6 uncomment   <code>location: 'history'</code> it should look like <a href="https://github.com/abuiles/ember-rails-week-1/blob/master/config/router.es6#L8">https://github.com/abuiles/ember-rails-week-1/blob/master/config/router.es6#L8</a></li>
</ol>


<p>EAKR also has a scaffold command try the following</p>

<pre><code>   $ rails g scaffold clients first_name:string last_name:string --ember
   $ rake db:migrate
</code></pre>

<p>Now navigate to <code>http://localhost:3000/clients</code> &ndash; Congrats! you have your first Ember app running.</p>

<h4>Don&rsquo;t fall behind subscribe to the Ember.js with Ruby on Rails list and stay updated.</h4>

<p><form action="http://emberenos.us7.list-manage.com/subscribe/post?u=103dd05cb2005f7b5485df96d&amp;id=25604cee1d" method="post" id="mc-embedded-subscribe-form" name="mc-embedded-subscribe-form" class="validate" target="_blank" novalidate><label for="mce-EMAIL">Subscribe.</label><input type="email" value="" name="EMAIL" class="email" id="mce-EMAIL" placeholder="email address" required><!-- real people should not fill this in and expect good things - do not remove this or risk form bot signups--> <div style="position: absolute; left: -5000px;"><input type="text" name="b_103dd05cb2005f7b5485df96d_25604cee1d" value=""></div><input type="submit" value="Subscribe" name="subscribe" id="mc-embedded-subscribe" class="button"></div></form></p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Getting Started With Ember.js and Ruby on Rails: Ember Appkit Rails]]></title>
    <link href="http://blog.abuiles.com/blog/2014/01/31/getting-started-with-ember-and-ruby-on-rails-using-ember-appkit-rails/"/>
    <updated>2014-01-31T09:46:28-02:00</updated>
    <id>http://blog.abuiles.com/blog/2014/01/31/getting-started-with-ember-and-ruby-on-rails-using-ember-appkit-rails</id>
    <content type="html"><![CDATA[<p><strong>This is the second post of my series about <a href="http://blog.abuiles.com/ember-js-with-Ruby-on-rails/">learning Ember.js
with Ruby on Rails</a>.</strong></p>

<h3>Ember App Kit</h3>

<p><a href="https://github.com/stefanpenner/ember-app-kit">Ember App Kit</a> is project started by Embereño <a href="http://twitter.com/stefanpenner">Stefan Penner</a>, from its page:</p>

<blockquote><p>Ember App Kit (EAK) is a robust starter kit for developing
applications in Ember.js. EAK makes it easy to develop, build, test,
and deploy applications independent of any back-end build process.</p></blockquote>

<p>Getting an Ember project started is really easy thanks to EAK, it
already puts the basic structure of our apps so we can focus on
what we like the most, developing.</p>

<p>But what if I want to use Ruby on Rails and Ember? How can I get all
this stuff to play nicely?.</p>

<p>There are three options:</p>

<ol>
<li>Keep two separate projects, a Rails app (probably using rails-api) and an EAK app.</li>
<li>Use the <code>ember-rails</code> which basically puts Ember&rsquo;s source files in your project and compiles your templates.</li>
<li>Use Ember Appkit Rails :).</li>
</ol>


<h3>Introducing Ember Appkit Rails.</h3>

<p><a href="https://github.com/dockyard/ember-appkit-rails">Ember Appkit Rails</a>
is a <a href="http://dockyard.com">DockYard&rsquo;s</a> project which aims to port
Ember App Kit to the Asset Pipeline.</p>

<p>Different to the <code>ember-rails</code>&rsquo;s gem, EAKR brings the same level of
importance to your Ember.js files as your Rails files (plus some other
goodies), and this has a huge impact on how we are used to look at Rails apps.</p>

<p>EAKR (Ember Appkit Rails) puts by default all your ember application
code under <code>app/</code> and configuration files under <code>config/</code>, it also
removes <code>app/assets/javascripts</code> from the assets load path and ignores
files put in that directory. All this can be configurable and
and could change for 1.0. (see [issue</p>

<h1>151](<a href="https://github.com/dockyard/ember-appkit-rails/issues/151">https://github.com/dockyard/ember-appkit-rails/issues/151</a>))</h1>

<p>You also get some Rails generators and if you pass
<code>--ember</code>, it will create the equivalent files for Ember.</p>

<p>As an example let&rsquo;s look at the output of <code>rails g scaffold posts --ember</code></p>

<pre><code>$rails g scaffold user first_name:string last_name:string --ember
      invoke  active_record
      create    db/migrate/20140131134237_create_users.rb
      create    app/models/user.rb
      invoke    rspec
      create      spec/models/user_spec.rb
      invoke      factory_girl
      create        spec/factories/users.rb
      create  config/serializers/user_serializer.rb
      invoke  ember:resource
      create    app/models/user.es6
      create    test/models/user_test.es6
      create    app/routes/users/edit.es6
      create    app/routes/users/index.es6
      create    app/routes/users/new.es6
      create    app/routes/users/show.es6
      insert    config/router.es6
      create    app/templates/users/edit.hbs
      create    app/templates/users/index.hbs
      create    app/templates/users/new.hbs
      create    app/templates/users/show.hbs
      create    app/templates/users.hbs
      create    app/templates/users/form.hbs
      invoke  scaffold_controller
      create    app/controllers/api/v1/users_controller.rb
      invoke    rspec
</code></pre>

<p>It creates our Ruby on Rails models, controllers, Ember files and test.</p>

<p>Some important things to notice here are</p>

<ol>
<li><p>Ember.js files are located under app, e.g. <code>app/models/user.es6</code></p></li>
<li><p>Note the extension for JavaScript files: <code>.es6</code>. It means that
they are written using ECMAScript 6 syntax which then is compiled to
AMD (Hopefully GitHub will <a href="https://github.com/github/linguist/pull/910">start to highlight this file</a>)</p></li>
<li><p>Your controller is namespaced under <code>api/vX</code>, you can change the version with <code>config.ember.api_version</code>, also your controller is optimized to be a <code>json</code> end-point.</p></li>
<li><p><code>test/models/user_test.es6</code> this is actually not part of the
official version but might be integrated any time soon (I&rsquo;m using a
development branch), you will also get test for other parts of your
Ember app like Routes, Controller, and Integration.</p></li>
</ol>


<p>All this is still under heavy development and some things might change
(I&rsquo;ll keep this post updated).</p>

<p>In the next post we will start to work on an application and also get
into some of Ember.js concepts as we start to come across them.</p>

<p><strong>Subscribe to the Ember.js with Ruby on Rails list to stay updated.</strong></p>

<p><form action="http://emberenos.us7.list-manage.com/subscribe/post?u=103dd05cb2005f7b5485df96d&amp;id=25604cee1d" method="post" id="mc-embedded-subscribe-form" name="mc-embedded-subscribe-form" class="validate" target="_blank" novalidate><label for="mce-EMAIL">Subscribe.</label><input type="email" value="" name="EMAIL" class="email" id="mce-EMAIL" placeholder="email address" required><!-- real people should not fill this in and expect good things - do not remove this or risk form bot signups--> <div style="position: absolute; left: -5000px;"><input type="text" name="b_103dd05cb2005f7b5485df96d_25604cee1d" value=""></div><input type="submit" value="Subscribe" name="subscribe" id="mc-embedded-subscribe" class="button"></div></form></p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Ruby on Rails MVC Is Not the Same as Ember.js MVC]]></title>
    <link href="http://blog.abuiles.com/blog/2014/01/27/ruby-on-rails-mvc-is-not-the-same-as-ember-dot-js-mvc/"/>
    <updated>2014-01-27T02:17:52-02:00</updated>
    <id>http://blog.abuiles.com/blog/2014/01/27/ruby-on-rails-mvc-is-not-the-same-as-ember-dot-js-mvc</id>
    <content type="html"><![CDATA[<p>One of the most confusing parts for people coming from Rails (or any
other backend web application framework) it&rsquo;s that what Ember.js
calls MVC is very different to what other frameworks call MVC.</p>

<p>To understand what MVC is in both frameworks I recommend watching
Yehuda Katz presentation:
<a href="http://www.youtube.com/watch?v=s1dhXamEAKQ">A tale of two MVC&rsquo;s</a>.</p>

<iframe width="853" height="480" src="http://blog.abuiles.com//www.youtube.com/embed/s1dhXamEAKQ" frameborder="0" allowfullscreen></iframe>


<p>Instead of talking about MVC, Yehuda proposes an unified model for GUI programming which is composed by a series of steps and then explains how different frameworks handle each step.</p>

<p>The following are the steps mention by Yehuda and the components in
charge of each in both Ruby on Rails and Ember.js.</p>

<h2>Step 1: Bootstrap Object</h2>

<p>Setup the initial state, bootstrap objects that you will require in
the future. e.g. Current User, Blog Posts.</p>

<ul>
<li>Ruby on Rails: Router and Controller</li>
<li>Ember.js: Route.</li>
</ul>


<h2>2. Draw Initial UI</h2>

<p>With the objects you setup on step 1 you will draw whatever the initial
UI is for your system.</p>

<ul>
<li>Ruby on Rails: View</li>
<li>Ember.js: Template</li>
</ul>


<p>** Views in Ember are not the same as Views in Rails, the
   equivalent are the templates, you will learn later what views are for.</p>

<h2>3. Translate Raw Input into User intent</h2>

<p>Once the user start to use the app, we want to interpret their intend,
for example clicking somewhere, pressing a key.</p>

<ul>
<li>Ruby on Rails: Template/Browser</li>
<li>Ember.js: Template or View.</li>
</ul>


<h2>4. Update application state</h2>

<p>As user starts to interact with the system, we need to update the
state of the app, for example we have an option box which is always in
the head, if the user click &ldquo;hide options&rdquo;, we need to
update the state of the app to reflect that and remember during the
rest of the user&rsquo;s session.</p>

<ul>
<li>Ruby on Rails: Controller (In the Session)</li>
<li>Ember.js: Route</li>
</ul>


<h2>5. Update Domain Objects</h2>

<p>If I remove a tweet for example, then I want to remove it from the system.</p>

<ul>
<li>Ruby on Rails: Controller</li>
<li>Ember.js: Controller</li>
</ul>


<h2>6. Notify UI Changes</h2>

<p>When there is something new, the UI needs to know somehow about it,
for example, there are new comments or the value for a field changed.</p>

<ul>
<li>Ruby on Rails: HTTP.</li>
<li>Ember.js: Data Binding.</li>
</ul>


<h2>7. Update the UI.</h2>

<p>New changes or whatever needs to be drawn is drawn.</p>

<ul>
<li>Ruby on Rails: Browser</li>
<li>Ember.js: Template</li>
</ul>


<p>The next time you are struggling to decide where to put something just look at the steps above and see where it belongs.</p>

<p>P.S I&rsquo;m <a href="http://blog.abuiles.com/ember-js-with-ruby-on-rails/">writing a series of post on working with Ember.js and Ruby on Rails</a>, subscribe to my list and I will let you know every time I publish an article on Ember.js and Ruby on Rails.</p>

<p><form action="http://emberenos.us7.list-manage.com/subscribe/post?u=103dd05cb2005f7b5485df96d&amp;id=25604cee1d" method="post" id="mc-embedded-subscribe-form" name="mc-embedded-subscribe-form" class="validate" target="_blank" novalidate><label for="mce-EMAIL">Subscribe.</label><input type="email" value="" name="EMAIL" class="email" id="mce-EMAIL" placeholder="email address" required><!-- real people should not fill this in and expect good things - do not remove this or risk form bot signups--> <div style="position: absolute; left: -5000px;"><input type="text" name="b_103dd05cb2005f7b5485df96d_25604cee1d" value=""></div><input type="submit" value="Subscribe" name="subscribe" id="mc-embedded-subscribe" class="button"></div></form></p>
]]></content>
  </entry>
  
</feed>
