<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Random Stuff, Ember.js mostly.]]></title>
  <link href="http://blog.abuiles.com/atom.xml" rel="self"/>
  <link href="http://blog.abuiles.com/"/>
  <updated>2014-11-18T08:23:05-05:00</updated>
  <id>http://blog.abuiles.com/</id>
  <author>
    <name><![CDATA[Adolfo Builes]]></name>
    <email><![CDATA[builes.adolfo@gmail.com]]></email>
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[Announcing My Ember Book: Ember-cli-101]]></title>
    <link href="http://blog.abuiles.com/blog/2014/11/16/announcing-my-ember-book-ember-cli-101/"/>
    <updated>2014-11-16T11:48:22-05:00</updated>
    <id>http://blog.abuiles.com/blog/2014/11/16/announcing-my-ember-book-ember-cli-101</id>
    <content type="html"><![CDATA[<p>In June I was traveling to Patagonia and had problems sleeping in the
plane. I opened notes in my iPhone and started to write what became
the first 2 chapters of an Ember.js book with ember-cli.</p>

<p>A lot of traveling came after that and then after finally settling
back in my home country, I decided it was time to continue. I started
to talk about it to friends and putting it on Twitter and Reddit but I
never did an official release until this day.</p>

<p>During the last months I&rsquo;ve been working on <a href="https://leanpub.com/ember-cli-101">ember-cli-101</a> which is a
book to teach people Ember, we build a real world application from
scratch to deployment, covering the 3 main components of the Ember
ecosystem: Ember, Ember-Data and ember-cli.</p>

<p>Things are moving fast in Ember world and that&rsquo;s why I have decided to
make this a <strong>living book</strong>, I&rsquo;ll keep pushing updates for new releases,
now with 2.0 coming, I&rsquo;ll start to edit the book so people write their
applications with 2.0 in mind.</p>

<p>I want to thank all of you who bought my book trusting I would finish
it and the whole Ember community for being supportive during this
time.</p>

<h2>Book + Online workshop</h2>

<p>As part of the book I&rsquo;m also doing an online workshop where I
personally give guidance to people reading the book + online sessions
of Q&amp;A, I plan to do 1 batch monthly, the first starts tomorrow, you
can enroll buying the workshop package in
<a href="https://leanpub.com/ember-cli-101">https://leanpub.com/ember-cli-101</a>.</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Understading Ember-Data Store#find]]></title>
    <link href="http://blog.abuiles.com/blog/2014/11/04/understading-ember-data-store-find/"/>
    <updated>2014-11-04T08:55:22-05:00</updated>
    <id>http://blog.abuiles.com/blog/2014/11/04/understading-ember-data-store-find</id>
    <content type="html"><![CDATA[<p><code>Store#find</code> (inject as <code>this.store.find</code>) is one of those methods in Ember-Data which
confuses people, especially since it behaves very different depending
on the parameters passed-in.</p>

<p>This article try to demystify <code>store.find</code> and explain how it will
behave accordingly to the parameters.</p>

<p>Let&rsquo;s have its definition on top and then use it as reference:</p>

<div>
  <pre><code class='javascript'>find: function(type, id, preload) {
    Ember.assert(&quot;You need to pass a type to the store&#39;s find method&quot;, arguments.length &gt;= 1);
    Ember.assert(&quot;You may not pass `&quot; + id + &quot;` as id to the store&#39;s find method&quot;, arguments.length === 1 || !Ember.isNone(id));

    if (arguments.length === 1) {
      return this.findAll(type);
    }

    // We are passed a query instead of an id.
    if (Ember.typeOf(id) === &#39;object&#39;) {
      return this.findQuery(type, id);
    }

    return this.findById(type, coerceId(id), preload);
}</code></pre>
</div>


<p>We can use <code>find</code> to load a single record or a collection of records.</p>

<p>If you want to run the commands in the article, you can download the
following ember-cli application <a href="https://github.com/abuiles/borrowers">https://github.com/abuiles/borrowers</a> and then
run <code>ember server</code> it will proxy all the API request to a public
API.</p>

<div>
  <pre><code class='bash'>$ git clone https://github.com/abuiles/borrowers
$ cd borrowers
$ npm install &amp;&amp; bower install</code></pre>
</div>


<h4>Scenario #1: Loading a collection</h4>

<p>If we call <code>find</code> only with a model&rsquo;s name then it will make a request
to load a list of records of that type, the following is an example of
such request:</p>

<div>
  <pre><code class='javascript'>friends =  $E.store.find(&#39;friend&#39;)

XHR finished loading: GET &quot;http://localhost:4200/api/v2/friends&quot;.</code></pre>
</div>


<p>First, in case you are wondering what <code>$E</code> is, I&rsquo;m grabbing an
instance of the application router using the <code>ember-inspector</code>.</p>

<p>If we want to send query parameters with our request, we can pass an
object as second argument and every key on the object will be included
as parameter:</p>

<div>
  <pre><code class='javascript'>friends =  $E.store.find(&#39;friend&#39;, {hasArticles: true, sort_by: &#39;created_at&#39;})

XHR finished loading: GET &quot;http://localhost:4200/api/v2/friends?hasArticles=true&amp;sort_by=created_at&quot;.</code></pre>
</div>


<p>In the previous request we asked <code>find</code> to load all the articles,
sending as parameters the key <code>hasArticles</code> and <code>sort_by</code>.</p>

<p>Like
<a href="http://emberjs.com/api/data/classes/DS.Store.html#method_all">all</a>
and
<a href="http://emberjs.com/api/data/classes/DS.Store.html#method_filter">filter</a>
the result from <code>find</code> is a <code>live array</code> too. When called it will make
a request to the server and then the collection will be updated if
more records are added or removed from the store.</p>

<p>I&rsquo;ve seen people using the private methods <code>findQuery</code> and <code>findByIds</code>
like:</p>

<div>
  <pre><code class='javascript'>// Using the private function store.findQuery
   model: function (params) {
      return this.store.findQuery(&#39;friend&#39;, {hasArticles: true, sort_by: &#39;created_at&#39;});
   }

   // Using the private funcion findByIds
   model: function (params) {
      return this.store.findByIds(&#39;friend&#39;, [1,2,3]);
   }</code></pre>
</div>


<p>We should never rely on private functions, they are marked private for
a reason. Although <a href="https://twitter.com/terzicigor">Igor Terzic</a>
pointed that <code>findQuery</code> might be marked as private by mistake.</p>

<p>We can achieve the same result using the method <code>find</code>:</p>

<div>
  <pre><code class='javascript'>model: function (params) {
      // bad
      // return this.store.findQuerygo(&#39;friend&#39;, {hasArticles: true, sort_by: &#39;created_at&#39;});

      // Good
      return this.store.find(&#39;friend&#39;, {hasArticles: true, sort_by: &#39;created_at&#39;})
   }

   model: function (params) {
      // bad
      // return this.store.findByIds(&#39;friend&#39;, [1,2,3]);

      // good
      return this.store.find(&#39;friend&#39;, {ids: [1,2,3]});
   }</code></pre>
</div>


<h4>Scenario #2: Loading a single record</h4>

<p>We can also use <code>find</code> to load an specific record, to do that we&rsquo;ll
only need to pass the record <code>id</code> as second argument:</p>

<div>
  <pre><code class='javascript'>$E.store.find(&#39;friend&#39;, 15)
XHR finished loading: GET &quot;http://localhost:4200/api/v2/friends/15&quot;.</code></pre>
</div>


<p>In the previous example we are loading the friend with id 15, the
<code>store</code> will only make a request to the server if the friend is not
available in the store, to understand this, let&rsquo;s go to
<a href="http://localhost:4200/friends">http://localhost:4200/friends</a> and then on the console try the following:</p>

<div>
  <pre><code class='javascript'>id = $E.store.all(&#39;friend&#39;).get(&#39;firstObject&#39;).id
$E.store.find(&#39;friend&#39;, id)</code></pre>
</div>


<p>If we open our network tab, we&rsquo;ll see that the store didn&rsquo;t make any request
this time, the reason is that we asked for a friend which was already
loaded into the store.</p>

<p>Is important to mention that <strong>find</strong>, <strong>all</strong> and <strong>filter</strong> return
promises, when testing on the browser&rsquo;s console we don&rsquo;t have to worry
about it, but if we want to use the result in our application then we
need to keep this in mind.</p>

<p><strong><a href="https://leanpub.com/ember-cli-101?utm_source=ed-store">Want to learn Ember.js? Enroll in my upcoming Ember.js workshop by buying my book</a></strong></p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Ember.js Debugging Tip: Use the Ember Inspector]]></title>
    <link href="http://blog.abuiles.com/blog/2014/10/28/ember-js-debugging-tip-use-ember-inspector/"/>
    <updated>2014-10-28T08:53:50-05:00</updated>
    <id>http://blog.abuiles.com/blog/2014/10/28/ember-js-debugging-tip-use-ember-inspector</id>
    <content type="html"><![CDATA[<p>We can grab any instance of a Route, Controller, View or Model
with the <strong>ember-inspector</strong> and then reference it in the console with
the <code>$E</code> variable.</p>

<p>This is very useful for debugging or making request to our API using
the <code>store</code>.</p>

<p>The following image shows you how to grab any instance using the ember-inspector.
<img src="http://cl.ly/image/2t2c2C0j0Z0C/ember-inspector-1.png" alt="ember-inspector" /></p>

<p>Happy Debugging!</p>

<p><a href="https://leanpub.com/ember-cli-101">Looking for an Ember.js tutor? Buy my Ember.js book with unlimited help through the IRC channel</a>.</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Working With JavaScript Plugins in Ember-cli: Part 2]]></title>
    <link href="http://blog.abuiles.com/blog/2014/10/17/working-with-javascript-plugins-in-ember-cli-part-2/"/>
    <updated>2014-10-17T09:16:04-05:00</updated>
    <id>http://blog.abuiles.com/blog/2014/10/17/working-with-javascript-plugins-in-ember-cli-part-2</id>
    <content type="html"><![CDATA[<p>On <a href="http://blog.abuiles.com/blog/2014/10/03/working-with-javascript-plugins-in-ember-cli/">part 1 of Working With JavaScript Plugins in
ember-cli</a>
, we explore how to work with JavaScript plugins which are still
distributed as globas and came up with a pattern for wrapping up
globals while using the module system.</p>

<p>In this article, we&rsquo;ll explore how to work with libraries which
include a named AMD distribution.</p>

<h2>Working with libraries with named AMD distributions.</h2>

<p>Before the addons system exist the easiest way to distribute
JavaScript libraries to be consume in ember-cli was to have a distribution
with a named-AMD version, then importing such library using <code>app.import</code>
and whitelisting the library&rsquo;s exports.</p>

<p>On of the libraries distributed in this way is
<a href="https://github.com/instructure/ic-ajax/tree/v2.0.1/lib">ic-ajax</a> an
&ldquo;Ember-friendly <code>jQuery.ajax</code> wrapper&rdquo;, if we navigate to the
<a href="https://github.com/instructure/ic-ajax/blob/master/lib/main.js">lib/main.js</a>
we&rsquo;ll notice that the source of the application is written with
<code>ES6</code> syntax but it is
<a href="https://github.com/instructure/ic-ajax/tree/v2.0.1/dist">distributed</a>
in different formats so you can consume it like a global or in a
module format.</p>

<p>As mentioned in the previously post <code>loader.js</code> doesn&rsquo;t work with anonymous AMD
distributions so if we want to include <code>ic-ajax</code> we need to use the
<code>named-amd</code> output.</p>

<p>Let&rsquo;s create a new ember-cli project like <code>ember new amd-example</code></p>

<p>Once the project has been created, let&rsquo;s go to the directory and run the following command</p>

<div>
  <pre><code class='bash'>npm uninstall ember-cli-ic-ajax --save-dev</code></pre>
</div>


<p>The library we just removed is an <code>ember-cli addon</code> which wraps all
the steps we are about to do but we won&rsquo;t be using it since we are
interested in learning how things are working under the hood and what
are we gaining when using the addon.</p>

<p>Next we need to add the library to bower, we can do so with <code>bower
install ic-ajax --save</code>, once installed let&rsquo;s import it in our
<code>Brocfile.js</code> as follows:</p>

<p> <div>
  <pre><code class='JavaScript'>app.import(&#39;bower_components/ic-ajax/dist/named-amd/main.js&#39;);</code></pre>
</div></p>

<p><code>ic-ajax</code> default export is the <code>request</code> function which allows us to
make request and manage them as if they were promises, we&rsquo;ll use
<code>request</code> to pull data from our backend.</p>

<p>We&rsquo;ll be using ember-cli&rsquo;s <code>proxy</code> to connect to a public API from my <a href="https://leanpub.com/ember-cli-101">Ember.js book: ember-cli 101</a>, pull some data and then render it in the index template.</p>

<p>Let&rsquo;s start by creating an index route with the following command: <code>ember g route index</code> and then put the following content in <code>app/routes/index.js</code>:</p>

<div>
  <pre><code class='JavaScript'>import Ember from &#39;ember&#39;;

//
// Here we are importing the default export from ic-ajax and assigning it to
// the variable request
//

import request from &#39;ic-ajax&#39;;

export default Ember.Route.extend({
  model: function()  {
    //
    // We&#39;ll be loading a collection from a resource called friends, it will
    // response with a JSON array which main key is friends
    //
    // We can see this response going to http://api.ember-cli-101.com/api/friends
    //

    return request(&#39;/api/friends&#39;).then(function(data){
      return {
        friendsCount: data.friends.length
      };
    });
  }
});</code></pre>
</div>


<p>And then replace <code>app/templates/index.hbs</code> so it uses
<code>friendsCount</code>:</p>

<figure class='code'><figcaption><span></span></figcaption><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
</pre></td><td class='code'><pre><code class='html'><span class='line'><span class="nt">&lt;h1&gt;</span>Total friends in api.ember-cli-101.com {{friendsCount}}<span class="nt">&lt;/h1&gt;</span>
</span></code></pre></td></tr></table></div></figure>


<p>The previous code is correct but we&rsquo;ll see an error when running ember server, try the following:</p>

<div>
  <pre><code class='bash'>$ ember server --proxy http://api.ember-cli-101.com
version: 0.0.46
Proxying to http://api.ember-cli-101.com
Livereload server on port 35729
Serving on http://0.0.0.0:4200
ENOENT, no such file or directory &#39;/borrowers/tmp/tree_merger-tmp_dest_dir-KIfHrFRc.tmp/ic-ajax.js&#39;
Error: ENOENT, no such file or directory &#39;/borrowers/tmp/tree_merger-tmp_dest_dir-KIfHrFRc.tmp/ic-ajax.js&#39;
...</code></pre>
</div>


<p>At the beginning of the article we mentioned that part of the process
on consuming named AMD libraries was to use <code>app.import</code> and
<code>whitelist</code> the library&rsquo;s exports without explaining what we meant
with the later.</p>

<p>During the build process all our files under <code>app/</code> go through a
transformation step where the <code>ES6 Modules</code> are converted to <code>AMD format</code>,
when something like the following is found <code>import request from
'ic-ajax';</code> internally the tool in charge of transpiling the code,
checks if that is something already registered in the module system
and if not it tries to find the module and convert it to the proper
format, in the previous scenario it will try to find a file called
<code>ic-ajax.js</code>, but since it is a library we are including externally
such file doesn&rsquo;t exist hence causing the build to fail.</p>

<p>Whitelisting in this context means telling the tool in charge of
transforming our <code>ES6 Modules</code> to <code>AMD</code> that whenever <code>import request from
'ic-ajax'</code> is found, then assume is already included so it doesn&rsquo;t
try to resolve it.</p>

<p>To do so we pass an option called exports to <code>app.import</code> which
whitelist <code>ic-ajax</code> and and its <code>exports</code>. The exports are also used to whitelist variables in <code>JSHint</code>.</p>

<p>In the <code>Brocfile.js</code>, let&rsquo;s replace the call to <code>import</code> with the
following:</p>

<div>
  <pre><code class='JavaScript'>app.import(&#39;bower_components/ic-ajax/dist/named-amd/main.js&#39;, {
  exports: {
    &#39;ic-ajax&#39;: [
      &#39;default&#39;,
      &#39;defineFixture&#39;,
      &#39;lookupFixture&#39;,
      &#39;raw&#39;,
      &#39;request&#39;,
    ]
  }
});</code></pre>
</div>


<p>If we run <code>ember server --proxy http://api.ember-cli-101.com</code> we&rsquo;ll see that everything works and we&rsquo;ll see the friends count in our index visiting
<a href="http://localhost:4200/">http://localhost:4200/</a></p>

<h3>ember-cli-ic-ajax</h3>

<p>We started the chapter by removing <code>ember-cli-ic-ajax</code> which is an
addon wrapping the call to import and include the exports for us, if
we inspect the
<a href="https://github.com/rwjblue/ember-cli-ic-ajax/blob/master/index.js#L18">index file in the addon</a>,
we&rsquo;ll notice that it has almost the same things we added to our
<code>Brocfile.js</code>.</p>

<p>Now that we understand how importing <code>named AMD libraries</code> works, we can
remove the <code>import</code> for <code>ic-ajax</code> from the <code>Brocfile.js</code> and use
it via the <code>addon</code>, let&rsquo;s run the following commands and then stop and
start the server, everything should work:</p>

<div>
  <pre><code class='bash'>$ bower uninstall ic-ajax --save
$ npm i ember-cli-ic-ajax --save</code></pre>
</div>


<h3>Protip</h3>

<p>Did you know ember-cli has an <code>.ember-cli</code> file where you can specify the default value for some commands? Instead of passing <code>--proxy</code> all the time to our <code>ember server</code> command, we can edit the <code>.ember-cli</code> to include the proxy option as follows:</p>

<div>
  <pre><code class='javascript'>{
  &quot;disableAnalytics&quot;: false,
  &quot;proxy&quot;: &quot;http://api.ember-cli-101.com&quot;
}</code></pre>
</div>


<p>With that we can run <code>ember server</code> and it will read the <code>--proxy</code> option from the <code>.ember-cli</code> file.</p>

<p><strong><a href="https://leanpub.com/ember-cli-101">Getting started with Ember.js? I&rsquo;m writing an Ember.js book, learn Ember.js</a> with <a href="https://leanpub.com/ember-cli-101">ember-cli-101</a>. Ping me on #ember-cli-101 in Freenode if you have any question about it</strong></p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Working With JavaScript Plugins in Ember-cli]]></title>
    <link href="http://blog.abuiles.com/blog/2014/10/03/working-with-javascript-plugins-in-ember-cli/"/>
    <updated>2014-10-03T12:09:10-05:00</updated>
    <id>http://blog.abuiles.com/blog/2014/10/03/working-with-javascript-plugins-in-ember-cli</id>
    <content type="html"><![CDATA[<p>In this post we&rsquo;ll use momentjs to write an Ember helpers called
<code>formatted-date</code> which we&rsquo;ll be using in our templates to render
dates as <code>September 28, 2014</code> instead of <code>Sun Sep 28 2014 04:58:30
GMT-0500</code></p>

<p><strong> This post is extracted from my Ember.js book: <a href="https://leanpub.com/ember-cli-101">ember-cli-101</a></strong></p>

<h2>Installing moment</h2>

<p>ember-cli uses Bower to manage frontend dependencies (we can also use
addons but that&rsquo;s a different story), the simplest way to consume a
library is to add it via bower and then use <code>app.import</code> in our
<code>Brocfile.js</code>.</p>

<p>Assuming you have ember-cli already install, create a project doing <code>ember new deps-demo</code>.</p>

<p>Once npm is done, install <code>moment</code> with</p>

<div>
  <pre><code class='bash'>$ bower install moment --save</code></pre>
</div>


<p>The option <code>--save</code> adds the dependency to our <code>bower.json</code>, we
should find something similar to <code>"moment": "~2.8.3"</code> (the version
might be different).</p>

<p>Next, let&rsquo;s import <code>moment</code>, to know which file to import we can go
to <code>bower_components/moment/</code> and we&rsquo;ll see that it has a
<code>moment.js</code> file which is the non-minified version of the library or
we can point to any of the version under the directory <code>min/</code>, for
now let&rsquo;s use the non-minified.</p>

<p>Let&rsquo;s add the following to our <code>Brocfile.js</code>:</p>

<figure class='code'><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
</pre></td><td class='code'><pre><code class=''><span class='line'>app.import('bower_components/moment/moment.js');</span></code></pre></td></tr></table></div></figure>


<p>Next, if we start our application with <code>ember server</code>, and navigate to
<a href="http://localhost:4200">http://localhost:4200</a>, open the console and
type &ldquo;moment&rdquo;, we should have access to the <code>moment</code> object.</p>

<p>With that we have successfully included our first JavaScript plugin,
but there are some gotchas we need to be aware of.</p>

<h2>It&rsquo;s a global!</h2>

<p>ember-cli fosters the use of <code>ES6 Modules</code>, so it feels like giving a
step backwards if we add a library and then use it through its global,
right?.</p>

<p>The sad news is that not all libraries are written in such way that
they can be consumed easily via a modules loader, and even so if there is
an <code>AMD</code> definition included in the library not all of them are
compatible with the modules loader used by <code>ember-cli</code>.</p>

<p>For example, <code>moment</code> includes an <code>AMD</code> version</p>

<div>
  <pre><code class='javascript'>...
} else if (typeof define === &#39;function&#39; &amp;&amp; define.amd) {
    define(&#39;moment&#39;, function (require, exports, module) {
        if (module.config &amp;&amp; module.config() &amp;&amp; module.config().noGlobal === true) {
            // release the global variable
            globalScope.moment = oldGlobalMoment;
        }

        return moment;
    });</code></pre>
</div>


<p>But unfortunately the stable version of loader.js doesn&rsquo;t support that
yet.</p>

<p>Other libraries do the following:</p>

<div>
  <pre><code class='JavaScript'>define( [], function() {
    return lib;
});</code></pre>
</div>


<p>That&rsquo;s is known as an anonymous module and even though it&rsquo;s syntax is
valid, loader.js doesn&rsquo;t support that either since it expects
named modules.</p>

<p>This whole problem is not really ember-cli&rsquo;s fault but the fact
everyone is building their libraries in different formats making hard
for consumers to use.</p>

<p>On the ember-cli side there is work in progress so in a near future
people will be able to use <code>moment</code> or other JavaScript libraries via
<code>import</code> but the integration is not ready yet. See
<a href="https://github.com/stefanpenner/ember-cli/issues/2177">ember-cli#2177</a>
for more info.</p>

<p>So, what can we do about it?</p>

<h2>Wrapping globals</h2>

<p>Instead of consuming globals directly let&rsquo;s wrap then in a helper
module which will help us foster the use of modules and also update or
replace easily <code>moment</code> once we have a way to load it via the module
loader.</p>

<p>First, let&rsquo;s create an utils file called <code>date-helpers</code>:</p>

<div>
  <pre><code class='bash'>$ ember g util date-helpers
installing
  create app/utils/date-helpers.js
installing
  create tests/unit/utils/date-helpers-test.js</code></pre>
</div>


<p>Replace <code>app/utils/date-helpers.js</code> with the following:</p>

<div>
  <pre><code class='javascript'>function formatDate(date, format) {
  return window.moment(date).format(format);
}

export default {
  formatDate
}</code></pre>
</div>


<p>Here we are wrapping the call to <code>moment#format</code> in the function
<code>format</code> which we can consume doing <code>import { formatDate } from
'utils/date-helpers';</code>, with that we are back to our idea of using
modules, and also we&rsquo;ll have the facility to update easily <code>moment</code>
when our loader is ready to load it.</p>

<p>Also if we decide to stop using <code>moment</code> and replace it with any
other library which does the same, we don&rsquo;t need to change our
consuming code since it doesn&rsquo;t care how <code>format-date</code> is being
implemented.</p>

<h2>Writing an Ember helper: formatted-date.</h2>

<p>Helpers are pieces of code that help us augment our templates, in this
case we want to write a helper to have a a date as a formatted string.</p>

<p><code>ember-cli</code> includes a generator for helpers too, so let&rsquo;s create
<code>formatted-date</code> with the command <code>ember g helper formatted-date</code>, and
then modify <code>app/helpers/formatted-date</code> so it consumes our format
function.</p>

<div>
  <pre><code class='javascript'>import Ember from &#39;ember&#39;;

// We are consuming the function defined in our utils/date-helpers.
import { formatDate } from &#39;../utils/date-helpers&#39;;

export default Ember.Handlebars.makeBoundHelper(function(date, format) {
  return formatDate(date, format);
});</code></pre>
</div>


<p>Once we have our helper defined, we can use it in our templates like</p>

<figure class='code'><figcaption><span></span></figcaption><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
</pre></td><td class='code'><pre><code class='html'><span class='line'>{{formatted-date createdAt &#39;LL&#39;}}
</span></code></pre></td></tr></table></div></figure>


<h1>Wrapping up</h1>

<p>This is the first one of a 3 series articles on consuming JavaScript
libraries, here we cover the first scenario about consuming libraries
which distribution can&rsquo;t be consumed with loader.js. Next we&rsquo;ll
explore and understand how to consume libraries distributed in AMD.</p>

<p><strong><a href="https://leanpub.com/ember-cli-101">Getting started with Ember.js? I&rsquo;m writing an Ember.js book, learn Ember.js and ember-cli</a> with <a href="https://leanpub.com/ember-cli-101">ember-cli-101</a>.</strong></p>

<p><strong>Looking for an Ember.js Coach? <a href="http://blog.abuiles.com/emberjs-consulting/">Hire me</a>  and I&rsquo;ll help your team stay in the flow with my unlimited support plan</strong></p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Ember.js Route Hooks]]></title>
    <link href="http://blog.abuiles.com/blog/2014/10/01/ember-dot-js-route-hooks/"/>
    <updated>2014-10-01T09:33:17-05:00</updated>
    <id>http://blog.abuiles.com/blog/2014/10/01/ember-dot-js-route-hooks</id>
    <content type="html"><![CDATA[<p>As part of my Ember.js Coaching business, I explain very often to
people how the Ember Route hooks work and the kind of stuff they can
do with them, a question that always pops up is: <strong>But in which order
are they called?</strong>.</p>

<p>To answer this I use an awesome JSBin created by <a href="https://twitter.com/rwjblue">Robert
Jackson (@rwjblue)</a> which shows exactly the order in which the hooks are called, I find myself looking at this from time to time and always find it super useful so I hope you do too, enjoy!</p>

<p><a class="jsbin-embed" href="http://jsbin.com/rolo/1/embed?html,css,js,output">Ember Route Hook Order</a><script src="http://static.jsbin.com/js/embed.js"></script></p>

<p>Don&rsquo;t forget to thank Robert (@rwjblue) for this!</p>

<p><strong> <a href="https://leanpub.com/ember-cli-101">Getting started with Ember.js? I&rsquo;m writing an Ember.js book, learn Ember.js and ember-cli</a> with <a href="https://leanpub.com/ember-cli-101">ember-cli-101</a>.</strong></p>

<p><strong>Looking for an Ember.js Coach? <a href="http://blog.abuiles.com/emberjs-consulting/">Hire me</a>  and I&rsquo;ll help your team stay in the flow with my unlimited support plan</strong></p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Ember-cli Anatomy]]></title>
    <link href="http://blog.abuiles.com/blog/2014/08/29/ember-cli-anatomy/"/>
    <updated>2014-08-29T13:30:05-05:00</updated>
    <id>http://blog.abuiles.com/blog/2014/08/29/ember-cli-anatomy</id>
    <content type="html"><![CDATA[<p>Every new developer coming to Ember.js is probably using ember-cli,
but I&rsquo;ve noticed that most of the time they are not fully aware of
ember-cli&rsquo;s main components and how they interact together so I took
this from my <a href="https://leanpub.com/ember-cli-101">ember-cli-101</a> book
where I try to explain it.</p>

<p><code>ember-cli</code> is a <code>Node.js</code> command line application sitting on top of
other libraries.</p>

<p>Its main component is <a href="https://github.com/broccolijs/broccoli">Broccoli</a>, which allows us to have fast builds,
<code>Broccoli</code> is a builder designed with the goal of keeping builds as
fast as possible (currently there are some issues on Mac which slows down the build time but there is work in progress to fix it).</p>

<p>When we run <code>ember server</code>, <code>Broccoli</code> compiles our project and put it
in a directory where it can be served using
<a href="http://expressjs.com/"><code>Express.js</code></a> which is a <code>Node.js</code> library.
<code>Express</code> is not only used to served files but also to extend
<code>ember-cli</code> functionality using its <code>middlewares</code>, an example of this
is the <code>http-proxy</code> which supports the <code>--proxy</code> option, allowing us
to develop against our development backend.</p>

<p>Testing is powered by <code>QUnit</code> and <code>Testem</code>, we can always navigate to
<code>http:/localhost:4200/tests</code> and our test will be run automatically.
We can also run Testem in <code>CI</code> or <code>--development</code> mode with the <code>ember
test</code> command. Currently there is just support for <code>QUnit</code> through an
<code>ember-cli add-on</code>, we will probably see support for other testing
frameworks and runners as people get familiar with the add-on system.</p>

<p><code>ember-cli</code> uses it&rsquo;s <a href="https://github.com/stefanpenner/ember-jj-abrams-resolver">own
resolver</a>
and has a different naming convention to <code>Ember.js's</code> defaults.</p>

<p><code>ember-cli</code> makes us write our application using <code>ES6 Modules</code>, then the code gets transpiled (compiled)[1] to <code>AMD</code>[2] and finally it is loaded with <code>loader.js</code> which is a minimalist <code>AMD</code> loader.</p>

<p>If you want to use coffee script you can use it but it is encouraged
to use plain JS and ES6 where possible.</p>

<p>Finally we need to cover <code>Broccoli</code>&rsquo;s plugins because without them,
<code>Broccoli</code> wouldn&rsquo;t be as helpful. Every transformation that your
files are going through, are done with a <code>Broccoli</code> plugin, e.g.
transpiling, minifying, finger-printing, uglifying. You can have your
own <code>Broccoli</code> plugins and plug it wherever you want in the build
process.</p>

<p><strong> <a href="https://leanpub.com/ember-cli-101">Learn how to write apps with Ember.js and ember-cli</a> with my <a href="https://leanpub.com/ember-cli-101">ember-cli-101 book</a>.</strong></p>

<ul>
<li>[1]: The transpiring process is done with <a href="https://github.com/esnext/es6-module-transpiler">es6-module-transpiler</a>.</li>
<li>[2]: To know more about <code>AMD</code> checkout <a href="https://github.com/amdjs/amdjs-api/wiki/AMD">their wiki</a></li>
</ul>

]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Lightning Fast Deployments With Rails (in the Wild).]]></title>
    <link href="http://blog.abuiles.com/blog/2014/07/08/lightning-fast-deployments-with-rails/"/>
    <updated>2014-07-08T16:30:18-05:00</updated>
    <id>http://blog.abuiles.com/blog/2014/07/08/lightning-fast-deployments-with-rails</id>
    <content type="html"><![CDATA[<p><strong><a href="https://leanpub.com/ember-cli-101">Getting started with Ember.js? I cover different deploying strategies in my Ember.js book</a></strong></p>

<p>A couple of months ago I wrote an article about deploying ember-cli
apps to heroku. Looking back, it was more a dirty hack to get things
working than a long term solution.</p>

<p>During Ember Conf  I had the opportunity to talk with some of the @yapplabs folks
about the method they were using to deploy their apps and
then Luke Melia gave an interesting talk on Rails Conf [1] about it.</p>

<p>Then, there was a blog post by Feifan Wang [2] implementing Luke&rsquo;s concepts, and I used
that as the backbone for my current solution.</p>

<p>Instead of prefixing the current deploy with timestamps, we put all
the files on the same dir to avoid cache busting. By default when
accessing the app in Rails, we&rsquo;ll serve serve the latest stable
release which is stored in Redis as &ldquo;release:index.html&rdquo;, or if the
user asks for the &ldquo;canary&rdquo; version (?version=canary) then serve the
latest known deployment.</p>

<p>The reason for including a &lsquo;canary&rsquo; release [3] is that I can test stuff
in production before making it available to the public, also being
able to serve other versions, means we can develop different features
and share the same &ldquo;staging&rdquo; app, and then just test by passing as
param the feature&rsquo;s shortSHA.</p>

<p>There are more advantages about this approach, check out Luke&rsquo;s video
for more enlightment!</p>

<h1>Generating the build.</h1>

<p>First let&rsquo;s examine the Brocfile, we just tell Broccoli to fingerprint our assets preprending our cloudfront url to all of them.
the final result would look something like <code>https://d29bb5msqib8gy.cloudfront.net/assets/vendor-6673dc1e2f6a7bece01002d43fae1b5b.js</code>.</p>

<figure class='code'><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
<span class='line-number'>2</span>
<span class='line-number'>3</span>
<span class='line-number'>4</span>
<span class='line-number'>5</span>
<span class='line-number'>6</span>
<span class='line-number'>7</span>
<span class='line-number'>8</span>
<span class='line-number'>9</span>
<span class='line-number'>10</span>
<span class='line-number'>11</span>
<span class='line-number'>12</span>
<span class='line-number'>13</span>
<span class='line-number'>14</span>
</pre></td><td class='code'><pre><code class=''><span class='line'>var EmberApp = require('ember-cli/lib/broccoli/ember-app');
</span><span class='line'>
</span><span class='line'>var app = new EmberApp({
</span><span class='line'>  // broccoli-asset-rev is now an ember-cli addon.
</span><span class='line'>  fingerprint: {
</span><span class='line'>    prepend: 'https://d29bb5msqib8gy.cloudfront.net/'
</span><span class='line'>  },
</span><span class='line'>  minifyCSS: {
</span><span class='line'>    enabled: true,
</span><span class='line'>    options: {}
</span><span class='line'>  }
</span><span class='line'>});
</span><span class='line'>
</span><span class='line'>module.exports = app.toTree();</span></code></pre></td></tr></table></div></figure>


<p>With that we get our desired &lsquo;dist&rsquo; output. Next we need to upload it
to S3 (we might be able to do this directly with ember-cli soon)</p>

<h2>Gruntfile</h2>

<p>Next we tell grunt to upload to S3 and push the generated index to
Redis. The default task publish the index as a canary release (meaning
it doesn&rsquo;t get served by default) just if the user explicitly requires
<code>?version=canary</code>. If we want to make the current version the
one that the users get served then we run <code>grunt
publish-release</code>.</p>

<p>Normally we&rsquo;ll run the default task on our CI server against staging,
and then when things get merged into production run the release task,
optionally we can run always the default task against production, in that way your
clients can try the latest version of the app before making it
available to everyone else.</p>

<figure class='code'><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
<span class='line-number'>2</span>
<span class='line-number'>3</span>
<span class='line-number'>4</span>
<span class='line-number'>5</span>
<span class='line-number'>6</span>
<span class='line-number'>7</span>
<span class='line-number'>8</span>
<span class='line-number'>9</span>
<span class='line-number'>10</span>
<span class='line-number'>11</span>
<span class='line-number'>12</span>
<span class='line-number'>13</span>
<span class='line-number'>14</span>
<span class='line-number'>15</span>
<span class='line-number'>16</span>
<span class='line-number'>17</span>
<span class='line-number'>18</span>
<span class='line-number'>19</span>
<span class='line-number'>20</span>
<span class='line-number'>21</span>
<span class='line-number'>22</span>
<span class='line-number'>23</span>
<span class='line-number'>24</span>
<span class='line-number'>25</span>
<span class='line-number'>26</span>
<span class='line-number'>27</span>
<span class='line-number'>28</span>
<span class='line-number'>29</span>
<span class='line-number'>30</span>
<span class='line-number'>31</span>
<span class='line-number'>32</span>
<span class='line-number'>33</span>
<span class='line-number'>34</span>
<span class='line-number'>35</span>
<span class='line-number'>36</span>
<span class='line-number'>37</span>
<span class='line-number'>38</span>
<span class='line-number'>39</span>
<span class='line-number'>40</span>
<span class='line-number'>41</span>
<span class='line-number'>42</span>
<span class='line-number'>43</span>
<span class='line-number'>44</span>
<span class='line-number'>45</span>
<span class='line-number'>46</span>
<span class='line-number'>47</span>
<span class='line-number'>48</span>
<span class='line-number'>49</span>
<span class='line-number'>50</span>
<span class='line-number'>51</span>
<span class='line-number'>52</span>
<span class='line-number'>53</span>
<span class='line-number'>54</span>
<span class='line-number'>55</span>
<span class='line-number'>56</span>
<span class='line-number'>57</span>
<span class='line-number'>58</span>
<span class='line-number'>59</span>
<span class='line-number'>60</span>
<span class='line-number'>61</span>
<span class='line-number'>62</span>
<span class='line-number'>63</span>
</pre></td><td class='code'><pre><code class=''><span class='line'>module.exports = function(grunt) {
</span><span class='line'>  grunt.initConfig({
</span><span class='line'>    env: grunt.file.readJSON('.env'),
</span><span class='line'>    s3: {
</span><span class='line'>      options: {
</span><span class='line'>        key: '&lt;%= env.AWS_ACCESS_KEY_ID %&gt;',
</span><span class='line'>        secret: '&lt;%= env.AWS_SECRET_ACCESS_KEY %&gt;',
</span><span class='line'>        bucket: '&lt;%= env.AWS_BUCKET %&gt;',
</span><span class='line'>        access: 'public-read',
</span><span class='line'>        headers: {
</span><span class='line'>          "Cache-Control": "max-age=630720000, public",
</span><span class='line'>          "Expires": new Date(Date.now() + 630720000).toUTCString()
</span><span class='line'>        },
</span><span class='line'>      },
</span><span class='line'>      dev: {
</span><span class='line'>        upload: [
</span><span class='line'>          {
</span><span class='line'>            src: 'dist/assets/**/*',
</span><span class='line'>            dest: 'assets/',
</span><span class='line'>            rel: 'dist/assets',
</span><span class='line'>            options: { verify: true }
</span><span class='line'>          }
</span><span class='line'>        ]
</span><span class='line'>      }
</span><span class='line'>    },
</span><span class='line'>    redis: {
</span><span class='line'>      options: {
</span><span class='line'>        manifestKey: 'releases',
</span><span class='line'>        manifestSize: 10,
</span><span class='line'>        host: '&lt;%= env.REDISTOGO.host %&gt;',
</span><span class='line'>        port: '&lt;%= env.REDISTOGO.port %&gt;',
</span><span class='line'>        connectionOptions: {
</span><span class='line'>          auth_pass: '&lt;%= env.REDISTOGO.password %&gt;'
</span><span class='line'>        }
</span><span class='line'>      },
</span><span class='line'>      canary: {
</span><span class='line'>        options: {
</span><span class='line'>          prefix: '&lt;%= gitinfo.local.branch.current.shortSHA %&gt;:',
</span><span class='line'>          currentDeployKey: '&lt;%= gitinfo.local.branch.current.shortSHA %&gt;',
</span><span class='line'>        },
</span><span class='line'>        files: {
</span><span class='line'>          src: ["dist/index.html"]
</span><span class='line'>        }
</span><span class='line'>      },
</span><span class='line'>      release: {
</span><span class='line'>        options: {
</span><span class='line'>          prefix: 'release:'
</span><span class='line'>        },
</span><span class='line'>        files: {
</span><span class='line'>          src: ["dist/index.html"]
</span><span class='line'>        }
</span><span class='line'>      }
</span><span class='line'>    },
</span><span class='line'>  });
</span><span class='line'>  grunt.loadNpmTasks('grunt-gitinfo');
</span><span class='line'>  grunt.loadNpmTasks('grunt-s3');
</span><span class='line'>  grunt.loadNpmTasks('grunt-redis');
</span><span class='line'>
</span><span class='line'>  grunt.registerTask('release', ['gitinfo', 'redis:release']);
</span><span class='line'>  grunt.registerTask('canary', ['gitinfo', 'redis:canary']);
</span><span class='line'>  grunt.registerTask('publish-release', ['default', 'release']);
</span><span class='line'>  return grunt.registerTask('default', ['gitinfo', 's3:dev', 'canary']);
</span><span class='line'>};</span></code></pre></td></tr></table></div></figure>


<h1>Rails</h1>

<p>In rails we will need to have <code>redis</code> gem installed and then
in our root action just serve the index.html from Redis.</p>

<figure class='code'><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
<span class='line-number'>2</span>
<span class='line-number'>3</span>
<span class='line-number'>4</span>
<span class='line-number'>5</span>
<span class='line-number'>6</span>
<span class='line-number'>7</span>
<span class='line-number'>8</span>
<span class='line-number'>9</span>
<span class='line-number'>10</span>
<span class='line-number'>11</span>
<span class='line-number'>12</span>
<span class='line-number'>13</span>
<span class='line-number'>14</span>
<span class='line-number'>15</span>
<span class='line-number'>16</span>
<span class='line-number'>17</span>
<span class='line-number'>18</span>
<span class='line-number'>19</span>
<span class='line-number'>20</span>
<span class='line-number'>21</span>
<span class='line-number'>22</span>
<span class='line-number'>23</span>
<span class='line-number'>24</span>
<span class='line-number'>25</span>
<span class='line-number'>26</span>
<span class='line-number'>27</span>
<span class='line-number'>28</span>
<span class='line-number'>29</span>
<span class='line-number'>30</span>
<span class='line-number'>31</span>
</pre></td><td class='code'><pre><code class=''><span class='line'>class LandingController &lt; ApplicationController
</span><span class='line'>  def index
</span><span class='line'>    render text: index_html
</span><span class='line'>  end
</span><span class='line'>
</span><span class='line'>  private
</span><span class='line'>
</span><span class='line'>  def index_html
</span><span class='line'>    redis.get "#{deploy_key}:index.html"
</span><span class='line'>  end
</span><span class='line'>
</span><span class='line'>  # By default serve release, if canary is specified then the latest
</span><span class='line'>  # known release, otherwise the requested version.
</span><span class='line'>  def deploy_key
</span><span class='line'>    params[:version] ||= 'release'
</span><span class='line'>    case params[:version]
</span><span class='line'>    when 'release' then 'release'
</span><span class='line'>    when 'canary'  then  redis.lindex('releases', 0)
</span><span class='line'>    else
</span><span class='line'>      params[:version]
</span><span class='line'>    end
</span><span class='line'>  end
</span><span class='line'>
</span><span class='line'>  def redis
</span><span class='line'>    if Rails.env.development?
</span><span class='line'>      redis = Redis.new()
</span><span class='line'>    else
</span><span class='line'>      Redis.new(:url =&gt; ENV['REDISTOGO_URL'])
</span><span class='line'>    end
</span><span class='line'>  end
</span><span class='line'>end</span></code></pre></td></tr></table></div></figure>


<p>And voilá! You have separated your Ember.js and Rails app with super fast deployments!</p>

<p>Dealing with CSRF can be done injecting the csrf token in your header
and then telling Ember to pick it up for you, or use my
<a href="https://github.com/abuiles/rails-csrf">rails-csrf</a> plugin which will
take care of everything, it just requires and end-point for
fetching the csrf token.</p>

<p>If you want to see this in a real app check the client-side part in
<a href="https://github.com/abuiles/facturas-client/blob/master/Gruntfile.js">facturas-client</a>
and the Rails part in <a href="https://github.com/abuiles/facturas/commit/391fcfad81ba8afcabf33bd349d05ba19e3073da">facturas Rails</a></p>

<h2>Standing on the shoulder of giants.</h2>

<p>Thanks Luke, for your Rails Conf talk!</p>

<p><img src="http://cl.ly/image/1Z3M2B1Q2X2a/Screen%20Shot%202014-07-08%20at%202.12.20%20PM.png" alt="Luke Melia" /></p>

<p>Thanks Feifan Wang for you blog post and <code>grunt-redis-manifest</code>.</p>

<h4>Pair with me.</h4>

<p>If you are looking for help with Ember.js/ember-cli/Rails, I do a free hour of
pairing every week, shoot me an email to <a href="&#109;&#97;&#105;&#x6c;&#x74;&#x6f;&#58;&#x62;&#117;&#105;&#x6c;&#101;&#x73;&#x2e;&#97;&#100;&#x6f;&#x6c;&#x66;&#x6f;&#x40;&#x67;&#109;&#97;&#x69;&#108;&#x2e;&#x63;&#111;&#x6d;">&#x62;&#117;&#x69;&#x6c;&#101;&#115;&#46;&#97;&#x64;&#111;&#x6c;&#x66;&#111;&#64;&#103;&#109;&#97;&#x69;&#108;&#x2e;&#99;&#x6f;&#109;</a>.</p>

<p><strong><a href="https://leanpub.com/ember-cli-101">Get my Ember.js book and to learn how to write apps with Ember.js, Ruby on Rails and ember-cli</a></strong></p>

<ul>
<li>[1] <a href="https://www.youtube.com/watch?v=QZVYP3cPcWQ">RailsConf 2014 &ndash; Lightning Fast Deployment of Your Rails-backed JavaScript app</a></li>
<li>[2] <a href="https://medium.com/@feifanw/framework-agnostic-fast-zero-downtime-javascript-app-deployment-df40cf105622">Framework agnostic, fast zero-downtime Javascript app deployment</a></li>
<li>[3] <a href="http://martinfowler.com/bliki/CanaryRelease.html">Canary Release</a></li>
</ul>

]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Deploying Ember-cli and Rails to Heroku]]></title>
    <link href="http://blog.abuiles.com/blog/2014/05/21/deploying-ember-cli-and-rails-to-heroku/"/>
    <updated>2014-05-21T08:09:55-05:00</updated>
    <id>http://blog.abuiles.com/blog/2014/05/21/deploying-ember-cli-and-rails-to-heroku</id>
    <content type="html"><![CDATA[<p><strong> this post is out-dated use <a href="http://blog.abuiles.com/blog/2014/07/08/lightning-fast-deployments-with-rails">Lightning Fast Deployments instead</a> </strong></p>

<p>I moved the app which I was doing as part of my Ember.js with Rails
series to ember-cli, but just until very recently deployed to heroku.</p>

<p>I want to put my ember-cli output into my <code>rails/public</code> and then
include the scripts into my application layout</p>

<figure class='code'><figcaption><span></span></figcaption><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
<span class='line-number'>2</span>
<span class='line-number'>3</span>
<span class='line-number'>4</span>
<span class='line-number'>5</span>
<span class='line-number'>6</span>
<span class='line-number'>7</span>
<span class='line-number'>8</span>
<span class='line-number'>9</span>
<span class='line-number'>10</span>
<span class='line-number'>11</span>
<span class='line-number'>12</span>
<span class='line-number'>13</span>
<span class='line-number'>14</span>
<span class='line-number'>15</span>
<span class='line-number'>16</span>
<span class='line-number'>17</span>
<span class='line-number'>18</span>
<span class='line-number'>19</span>
<span class='line-number'>20</span>
</pre></td><td class='code'><pre><code class='html'><span class='line'><span class="cp">&lt;!DOCTYPE html&gt;</span>
</span><span class='line'><span class="nt">&lt;html&gt;</span>
</span><span class='line'>  <span class="nt">&lt;head&gt;</span>
</span><span class='line'>    <span class="nt">&lt;base</span> <span class="na">href=</span><span class="s">&quot;/&quot;</span> <span class="nt">/&gt;</span>
</span><span class='line'>
</span><span class='line'>    <span class="nt">&lt;link</span> <span class="na">rel=</span><span class="s">&quot;stylesheet&quot;</span> <span class="na">href=</span><span class="s">&quot;ember-assets/vendor.css&quot;</span><span class="nt">&gt;</span>
</span><span class='line'>    <span class="nt">&lt;link</span> <span class="na">rel=</span><span class="s">&quot;stylesheet&quot;</span> <span class="na">href=</span><span class="s">&quot;ember-assets/client.css&quot;</span><span class="nt">&gt;</span>
</span><span class='line'>    <span class="err">&lt;</span>%= csrf_meta_tags %&gt;
</span><span class='line'>  <span class="nt">&lt;/head&gt;</span>
</span><span class='line'>  <span class="nt">&lt;body&gt;</span>
</span><span class='line'>    <span class="err">&lt;</span>%= yield %&gt;
</span><span class='line'>    <span class="nt">&lt;script&gt;</span>
</span><span class='line'>      <span class="nb">window</span><span class="p">.</span><span class="nx">ENV</span> <span class="o">=</span> <span class="p">{</span><span class="s2">&quot;railsCsrf&quot;</span><span class="o">:</span><span class="p">{</span><span class="s2">&quot;csrfURL&quot;</span><span class="o">:</span><span class="s2">&quot;api/v1/csrf&quot;</span><span class="p">},</span><span class="s2">&quot;baseURL&quot;</span><span class="o">:</span><span class="s2">&quot;/&quot;</span><span class="p">,</span><span class="s2">&quot;locationType&quot;</span><span class="o">:</span><span class="s2">&quot;auto&quot;</span><span class="p">,</span><span class="s2">&quot;FEATURES&quot;</span><span class="o">:</span><span class="p">{</span><span class="s2">&quot;query-params-new&quot;</span><span class="o">:</span><span class="kc">true</span><span class="p">},</span><span class="s2">&quot;APP&quot;</span><span class="o">:</span><span class="p">{}};</span>
</span><span class='line'>    <span class="nt">&lt;/script&gt;</span>
</span><span class='line'>    <span class="nt">&lt;script </span><span class="na">src=</span><span class="s">&quot;ember-assets/client.js&quot;</span><span class="nt">&gt;&lt;/script&gt;</span>
</span><span class='line'>    <span class="nt">&lt;script&gt;</span>
</span><span class='line'>      <span class="nb">window</span><span class="p">.</span><span class="nx">Client</span> <span class="o">=</span> <span class="nx">require</span><span class="p">(</span><span class="s1">&#39;client/app&#39;</span><span class="p">)[</span><span class="s1">&#39;default&#39;</span><span class="p">].</span><span class="nx">create</span><span class="p">(</span><span class="nx">ENV</span><span class="p">.</span><span class="nx">APP</span><span class="p">);</span>
</span><span class='line'>    <span class="nt">&lt;/script&gt;</span>
</span><span class='line'>  <span class="nt">&lt;/body&gt;</span>
</span><span class='line'><span class="nt">&lt;/html&gt;</span>
</span></code></pre></td></tr></table></div></figure>


<p>I do it like this because I want to include my <code>csrf_tags</code> in the body
and also have the option of preloading any data if I need to.</p>

<p>Out of topic, but since handling csrf-tags is such a common scenario I
wrote <a href="http://github.com/abuiles/rails-csrf">rails-csrf</a>, a plugin
which you can install through bower for handling CSRF for you.</p>

<h2>Building</h2>

<p>I came through a script by Jason Madsen(@jason_madsen) which would
take care of building the ember-cli app and putting it under public,
it works great but the final output is under your <code>public/index.html</code>
which I don&rsquo;t want.</p>

<p>I modified the script to do a couple of things more.</p>

<ol>
<li>Add <code>&lt;%= csrf_meta_tags %&gt;</code> before head (line 40).</li>
<li>Add <code>&lt;%= yield %&gt;</code> after body (line 43).</li>
<li>Replace <code>application.html.erb</code> with my final <code>index.html</code> (line 46)</li>
</ol>


<p>The script can be found on the following <a href="https://gist.github.com/abuiles/5c281123fc41e6b988e3">gist</a></p>

<h2>Deploying</h2>

<p>Now with the desired output, is just a matter of <code>git commit</code> and <code>git push</code> to heroku.</p>

<p>To make it easier I added the following task to my Rakefile, it:</p>

<ol>
<li>Accepts the &ldquo;env&rdquo; I want to deploy and the remote name (heroku end-point).</li>
<li>Moves to the specified env and merges master.</li>
<li>Runs the <code>build</code> script and commit the changes.</li>
<li>Push the changes to heroku.</li>
</ol>


<figure class='code'><figcaption><span></span></figcaption><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
<span class='line-number'>2</span>
<span class='line-number'>3</span>
<span class='line-number'>4</span>
<span class='line-number'>5</span>
<span class='line-number'>6</span>
<span class='line-number'>7</span>
<span class='line-number'>8</span>
<span class='line-number'>9</span>
<span class='line-number'>10</span>
</pre></td><td class='code'><pre><code class='ruby'><span class='line'><span class="n">task</span> <span class="ss">:deploy</span><span class="p">,</span> <span class="ss">:env</span><span class="p">,</span> <span class="ss">:remote</span> <span class="k">do</span> <span class="o">|</span><span class="n">t</span><span class="p">,</span> <span class="n">args</span><span class="o">|</span>
</span><span class='line'>  <span class="n">sh</span> <span class="s2">&quot;git checkout </span><span class="si">#{</span><span class="n">args</span><span class="o">[</span><span class="ss">:env</span><span class="o">]</span><span class="si">}</span><span class="s2">&quot;</span>
</span><span class='line'>  <span class="n">sh</span> <span class="s1">&#39;git merge master -m &quot;Merging for deployment&quot;&#39;</span>
</span><span class='line'>  <span class="n">sh</span> <span class="s1">&#39;./build.sh&#39;</span>
</span><span class='line'>
</span><span class='line'>  <span class="n">sh</span> <span class="s1">&#39;git add -A&#39;</span>
</span><span class='line'>  <span class="n">sh</span> <span class="s1">&#39;git commit -m &quot;Compile for deployment&quot;&#39;</span>
</span><span class='line'>
</span><span class='line'>  <span class="n">sh</span> <span class="s2">&quot;git push </span><span class="si">#{</span><span class="n">args</span><span class="o">[</span><span class="ss">:remote</span><span class="o">]</span><span class="si">}</span><span class="s2"> head:master&quot;</span>
</span><span class='line'><span class="k">end</span>
</span></code></pre></td></tr></table></div></figure>


<p>A standard deployment would be something like <code>rake deploy[production, heroku]</code></p>

<p>It assumes you have a branch called <code>production</code> and that your remote is called <code>heroku</code>.</p>

<h2>Acknowledgements</h2>

<p>Thanks to Jason Madsen for his initial script, he also made a rails template <a href="https://github.com/knomedia/ember-cli-rails">knomedia/ember-cli-rails</a>.</p>

<p>The rake task was mostly inspired by <a href="https://github.com/dockyard/ember-cli-plus-backend">DockYard&rsquo;s ember-cli-plus-backend</a>.</p>

<h2>Future work</h2>

<p>There is still a lot of things to improve which I&rsquo;ll be blogging about as part of my ember-cli + Rails series. Here&rsquo;s a list of things:</p>

<ol>
<li>Deploying to heroku just the Rails files, currently we include the ember-cli app as well.</li>
<li>Using cloud front for serving your assets.</li>
<li><del>Fingerprinting</del> is super simple with <a href="https://github.com/rickharrison/broccoli-asset-rev">broccoli-asset-rev</a> see it in use <a href="https://github.com/abuiles/facturas/commit/12b23d5fc36d0906a1e21cae7aed97bdbb8cba9e">here</a>.</li>
<li>Doing lightning fast deployments (see <a href="http://www.confreaks.com/videos/3324-railsconf-lightning-fast-deployment-of-your-rails-backed-javascript-app">Luke&rsquo;s Melia talk on Rails Conf</a>).</li>
</ol>


<p><strong><a href="http://blog.abuiles.com/ember-rails/">Learn how to write apps with Ember.js, ember-cli and Ruby on Rails</a>, subscribe to my list and I will let you know every time I publish an article. </strong></p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Ember-cli + Rails]]></title>
    <link href="http://blog.abuiles.com/blog/2014/05/12/ember-cli-plus-rails/"/>
    <updated>2014-05-12T18:42:36-05:00</updated>
    <id>http://blog.abuiles.com/blog/2014/05/12/ember-cli-plus-rails</id>
    <content type="html"><![CDATA[<p>I stopped writing my series of getting started with Ember.js using
Ember AppKit Rails after finding out that the gem was doomed to die in
favor of using ember-cli. Brian Cardarella <a href="http://reefpoints.dockyard.com/2014/05/04/stop-using-ember-appkit-rails.html">wrote</a> recently about the
reasons behind.</p>

<p>During the last months I&rsquo;ve been following ember-cli and using it to
migrate the app which I was building as part of the series for then
writing about it, but Brian released recently a series of post about
using ember-cli with Rails which are really good:</p>

<ol>
<li> <a href="http://reefpoints.dockyard.com/2014/05/07/building-an-ember-app-with-rails-part-1.html">Building an Ember App with Rails Part 1.</a></li>
<li> <a href="http://reefpoints.dockyard.com/2014/05/08/building-an-ember-app-with-rails-part-2.html">Building an Ember App with Rails Part 2.</a></li>
<li> <a href="http://reefpoints.dockyard.com/2014/05/09/building-an-ember-app-with-rails-part-3.html">Building an Ember App with Rails Part 2.</a></li>
</ol>


<p>I still plan to continue writing about it since there is room for a
lot of stuff and the ember-cli project is moving fast, but for now I
would recommend Brian&rsquo;s posts and as he said, just stopped using EAKR,
ember-cli is the future and building apps with it is a lot of
fun.</p>

<p>If you are interested in migrating an app from Rails to ember-cli the
following are the commits since I started the migration <a href="https://github.com/abuiles/facturas/compare/b47781f...master">https://github.com/abuiles/facturas/compare/b47781f&hellip;master</a>.</p>

<p><strong>I&rsquo;m still <a href="http://blog.abuiles.com/ember-rails/">writing about working with Ember.js and Ruby on Rails now with ember-cli</a>, subscribe to my list and I will let you know every time I publish an article.</strong></p>

<p><form action="http://emberenos.us7.list-manage.com/subscribe/post?u=103dd05cb2005f7b5485df96d&amp;id=25604cee1d" method="post" id="mc-embedded-subscribe-form" name="mc-embedded-subscribe-form" class="validate" target="_blank" novalidate><label for="mce-EMAIL">Subscribe.</label><input type="email" value="" name="EMAIL" class="email" id="mce-EMAIL" placeholder="email address" required><!-- real people should not fill this in and expect good things - do not remove this or risk form bot signups--> <div style="position: absolute; left: -5000px;"><input type="text" name="b_103dd05cb2005f7b5485df96d_25604cee1d" value=""></div><input type="submit" value="Subscribe" name="subscribe" id="mc-embedded-subscribe" class="button"></div></form></p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Handling Errors in Ember.js and Ruby on Rails]]></title>
    <link href="http://blog.abuiles.com/blog/2014/04/14/handling-errors-in-ember-dot-js-and-ruby-on-rails/"/>
    <updated>2014-04-14T21:39:22-05:00</updated>
    <id>http://blog.abuiles.com/blog/2014/04/14/handling-errors-in-ember-dot-js-and-ruby-on-rails</id>
    <content type="html"><![CDATA[<p>Ember.js has built-in support to handle errors returned by the backend through the computed property <code>errors</code>, to get it working with the <code>ActiveModelAdapter</code> you payload&rsquo;s root key has to be called <code>errors</code> and your status code should be <code>422</code>.</p>

<p>Suppose you want to create a post and you get an error like the following:</p>

<pre><code>{ errors: { body: ['can't be blank'] } }
</code></pre>

<p>Once the requests has been completed you can check if you model is valid or not with <code>model.get('isValid')</code> and then the  <code>errors</code> with:</p>

<pre><code> post.get('errors').get('body')
 [
   Object
   attribute: "body"
   message: "can't be blank"
   __proto__: Object
 ]
</code></pre>

<p>Notice that a record becomes <code>isInvalid</code> only if <code>DS.InvalidError</code> can be created, on the <code>ActiveModelAdapter</code> case that is if the status code is <code>422</code> and <code>errors</code> is the root of your payload, otherwise it becomes <code>isError</code>.</p>

<p>If you want a validation library checkout <a href="https://github.com/dockyard/ember-validations">DockYard&rsquo;s ember-validations</a></p>

<p>P.S I&rsquo;m <a href="http://blog.abuiles.com/ember-rails/">writing a series of post on working with Ember.js and Ruby on Rails</a>, subscribe to my list and I will let you know every time I publish an article on Ember.js and Ruby on Rails.</p>

<p><form action="http://emberenos.us7.list-manage.com/subscribe/post?u=103dd05cb2005f7b5485df96d&amp;id=25604cee1d" method="post" id="mc-embedded-subscribe-form" name="mc-embedded-subscribe-form" class="validate" target="_blank" novalidate><label for="mce-EMAIL">Subscribe.</label><input type="email" value="" name="EMAIL" class="email" id="mce-EMAIL" placeholder="email address" required><!-- real people should not fill this in and expect good things - do not remove this or risk form bot signups--> <div style="position: absolute; left: -5000px;"><input type="text" name="b_103dd05cb2005f7b5485df96d_25604cee1d" value=""></div><input type="submit" value="Subscribe" name="subscribe" id="mc-embedded-subscribe" class="button"></div></form></p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Router, Routes and Templates]]></title>
    <link href="http://blog.abuiles.com/blog/2014/02/21/router-routes-and-templates/"/>
    <updated>2014-02-21T08:01:26-05:00</updated>
    <id>http://blog.abuiles.com/blog/2014/02/21/router-routes-and-templates</id>
    <content type="html"><![CDATA[<p><strong>This post is part of my series about <a href="http://blog.abuiles.com/ember-js-with-ruby-on-rails">learning Ember.js with Ruby on Rails</a>.</strong></p>

<p>We have now the basic skeleton to <a href="http://blog.abuiles.com/blog/2014/02/07/building-an-app-for-my-mom-with-ember-appkit-rails/">start building an app for my
mom</a>,
today we are going to talk about 3 important concepts in Ember.js: The
<code>Router</code>, <code>Routes</code> and <code>Templates</code>.</p>

<p>Grab the application from where we left last:</p>

<pre><code>  $ git clone git@github.com:abuiles/ember-rails-week-1.git
  $ git reset --hard week-1
  $ bundle install
  $ rails s
</code></pre>

<p>Go to <code>http://localhost:3000/</code> and you should see &ldquo;Welcome to Ember on Rails!&rdquo;.</p>

<h2>Creating the Application Template.</h2>

<p>Ember&rsquo;s equivalent of Ruby on Rails Application layout is called application too.</p>

<p>Create the file <code>app/templates/application.hbs</code> and put the following content there.</p>

<figure class='code'><figcaption><span></span></figcaption><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
<span class='line-number'>2</span>
</pre></td><td class='code'><pre><code class='html'><span class='line'><span class="nt">&lt;h1&gt;</span>Hi Ember.js<span class="nt">&lt;/h1&gt;</span>
</span><span class='line'>{{outlet}}
</span></code></pre></td></tr></table></div></figure>


<p>Refresh your browser and you will see <code>Hi Ember.js</code>.</p>

<p>{{outlet}} is similar to <code>yield</code> on the application
template in Ruby on Rails <strong>but is not the same, there is also the
word <code>yield</code> on Ember.js we&rsquo;ll cover later</strong></p>

<p>Let&rsquo;s add something into the outlet, create the file
<code>app/templates/index.hbs</code> with the following</p>

<figure class='code'><figcaption><span></span></figcaption><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
</pre></td><td class='code'><pre><code class='html'><span class='line'><span class="nt">&lt;h1&gt;</span>I&#39;m the Index!<span class="nt">&lt;/h1&gt;</span>
</span></code></pre></td></tr></table></div></figure>


<p>Refresh your browser and you will see &ldquo;Hi Ember.js&rdquo; follow by &ldquo;I&rsquo;m the Index&rdquo;.</p>

<h2>Under the hood.</h2>

<p>To understand why the previous stuff worked without requiring us to
add any extra code we&rsquo;ll have to talk about the concept of <code>Routes</code>.</p>

<p>In Ember.js there is a <code>Router</code> and many <code>Routes</code>.</p>

<p><code>Router</code> is where we specify the entry points for our application,
this is similar to <code>routes.rb</code>.</p>

<p>In Rails <code>router.rb</code> every entry matches to an <code>action</code> in a
controller, this is not the case in Ember.js, we mentioned previously
that a <code>controller</code> in Ember acts more as decorators for our views.</p>

<p>In Ember every entry in the <code>Router</code> matches to a <code>Route</code>, if we have
in the <code>router.es6</code> an entry like:</p>

<figure class='code'><figcaption><span></span></figcaption><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
</pre></td><td class='code'><pre><code class='javascript'><span class='line'><span class="k">this</span><span class="p">.</span><span class="nx">route</span><span class="p">(</span><span class="s2">&quot;about&quot;</span><span class="p">,</span> <span class="p">{</span> <span class="nx">path</span><span class="o">:</span> <span class="s2">&quot;/about&quot;</span> <span class="p">});</span>
</span></code></pre></td></tr></table></div></figure>


<p>Then it will look for <code>app/routes/about.es6</code> and the template <code>app/templates/about.hbs</code>.</p>

<p>If Ember doesn&rsquo;t find the <code>route</code> it will create one automatically and
if we need to do any kind of processing before showing our template
like fetch data from a server or render a different template, then we
can do that on the <code>route</code> file.</p>

<p>With this in mind, the reason why putting <code>index.hbs</code> works without
doing anything extra is that Ember.js creates by default an
<code>IndexRoute</code>, which matches to <code>/</code>.</p>

<p>When we visit <code>/</code>, it tries to find <code>app/routes/index.es6</code>, if it is not there it
creates one by default and then renders the template under <code>templates/index.hbs</code>.</p>

<h2>A first Route</h2>

<p>Let&rsquo;s create a simple route for <code>index</code>, put the following under <code>/app/routes/index.es6</code>.</p>

<figure class='code'><figcaption><span></span></figcaption><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
<span class='line-number'>2</span>
<span class='line-number'>3</span>
<span class='line-number'>4</span>
<span class='line-number'>5</span>
</pre></td><td class='code'><pre><code class='javascript'><span class='line'><span class="kr">export</span> <span class="k">default</span> <span class="nx">Ember</span><span class="p">.</span><span class="nx">Route</span><span class="p">.</span><span class="nx">extend</span><span class="p">({</span>
</span><span class='line'>  <span class="nx">model</span><span class="o">:</span> <span class="kd">function</span><span class="p">()</span> <span class="p">{</span>
</span><span class='line'>    <span class="k">return</span> <span class="p">{</span> <span class="nx">date</span><span class="o">:</span> <span class="nb">Date</span><span class="p">()</span> <span class="p">};</span>
</span><span class='line'>  <span class="p">}</span>
</span><span class='line'><span class="p">});</span>
</span></code></pre></td></tr></table></div></figure>


<p>Replace the <code>index</code> template with</p>

<figure class='code'><figcaption><span></span></figcaption><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
</pre></td><td class='code'><pre><code class='html'><span class='line'><span class="nt">&lt;h1&gt;</span>I&#39;m the Index! {{date}}<span class="nt">&lt;/h1&gt;</span>
</span></code></pre></td></tr></table></div></figure>


<p>After refreshing you will see <code>I'm the Index</code> follow by a date, we
just write our first route, bootstrapped the required stuff for our
template and then rendered it into the template!</p>

<h2>Task</h2>

<p>We want to have an <code>/about</code> page, add the necessary entry to the
<code>Router</code> and template.</p>

<h2>Resources</h2>

<p>I suggest going to the official Ember.js guides and check the section for Routing and Templates.</p>

<ul>
<li><a href="http://emberjs.com/guides/routing/">Routing guide</a></li>
<li><a href="http://emberjs.com/guides/templates/the-application-template">Templates guide</a></li>
</ul>


<h4>Want to learn more about  Ember.js with Ruby on Rails? Subscribe</h4>

<p><form action="http://emberenos.us7.list-manage.com/subscribe/post?u=103dd05cb2005f7b5485df96d&amp;id=25604cee1d" method="post" id="mc-embedded-subscribe-form" name="mc-embedded-subscribe-form" class="validate" target="_blank" novalidate><label for="mce-EMAIL">Subscribe.</label><input type="email" value="" name="EMAIL" class="email" id="mce-EMAIL" placeholder="email address" required><!-- real people should not fill this in and expect good things - do not remove this or risk form bot signups--> <div style="position: absolute; left: -5000px;"><input type="text" name="b_103dd05cb2005f7b5485df96d_25604cee1d" value=""></div><input type="submit" value="Subscribe" name="subscribe" id="mc-embedded-subscribe" class="button"></div></form></p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Building an App for My Mom With Ember Appkit Rails.]]></title>
    <link href="http://blog.abuiles.com/blog/2014/02/07/building-an-app-for-my-mom-with-ember-appkit-rails/"/>
    <updated>2014-02-07T07:56:18-05:00</updated>
    <id>http://blog.abuiles.com/blog/2014/02/07/building-an-app-for-my-mom-with-ember-appkit-rails</id>
    <content type="html"><![CDATA[<p><strong>This post is part of my series about <a href="http://blog.abuiles.com/ember-js-with-ruby-on-rails">learning Ember.js with Ruby on Rails</a>.</strong></p>

<p>In previous post we explored what is
<a href="http://blog.abuiles.com/blog/2014/01/27/ruby-on-rails-mvc-is-not-the-same-as-ember-dot-js-mvc/">Ember.js MVC</a>
and then did a
<a href="http://blog.abuiles.com/blog/2014/01/31/getting-started-with-ember-and-ruby-on-rails-using-ember-appkit-rails/">high level overview of Ember Appkit Rails</a>,
in this post we&rsquo;ll start to work on a project which we will use as
reference for upcoming posts.</p>

<h2>The Problem</h2>

<p>My mother lives in a small town in Colombia where she owns a store,
access to credit cards in Colombia is really really hard, so stores
and people in small towns tend to buy and sell on credit, trusting
that people will come back and pay them (crazy, isn&rsquo;t?).</p>

<p>During my last visit I was really shocked when I saw that she was
keeping all her records on physical invoices in under an A-Z folder.</p>

<p>If <code>John Smith</code> takes a t-shirt on credit, they would create an
invoice with his name, add a description (e.g. t-shirt) with the price
<code>$25</code> and then throw that into the A-Z in the section for surnames
starting with <code>S</code>.</p>

<p>Later John returns to the store and makes a partial payment (e.g.
<code>$10</code>) or pays the whole thing.</p>

<p>This approach is horrible, insecure, my mother doesn&rsquo;t know how much
is people really owing her, she can&rsquo;t have insights, etc.</p>

<p>Let&rsquo;s then  build an App for my mother where she can keep her records, the app name will be <code>facturas</code> which means <code>invoices</code> in Spanish.</p>

<h2>Starting the project.</h2>

<p>For this project we will use <code>Ruby on Rails 4.1.0.beta1</code> and <code>Ember.js 1.4.0-beta</code>.</p>

<p>Install Ruby on Rails <code>gem install rails --pre</code>, once it&rsquo;s completed
run <code>rails new facturas</code> and we should have the basic structure for
our project.</p>

<p>Let&rsquo;s keep everything under <code>git</code> so we can keep track of changes.</p>

<pre><code>  $ git init
  $ git add .
  $ git commit -m 'Getting started!.'
</code></pre>

<p>I use <a href="https://github.com/github/hub">hub</a> so I will just push this to a new repo on GitHub.</p>

<pre><code>  $ hub create ember-rails-week-1
    Updating origin
    created repository: abuiles/ember-rails-week-1
  $ git push origin master
</code></pre>

<p>Install <code>Ember Appkit Rails</code>, since I want to use a new feature we
have been working on to include testing out of the box, we won&rsquo;t
install from rubygems but directly from GitHub.</p>

<p>Add the following to you Gemfile:</p>

<pre><code>gem 'ember-appkit-rails', git: 'https://github.com/dockyard/ember-appkit-rails.git'
</code></pre>

<p>Run <code>bundle install</code>.</p>

<p>Now we can bootstrap our Rails project with Ember Appkit Rails.</p>

<pre><code>  $ rails g ember:bootstrap
</code></pre>

<p>For now just focus on this two files:</p>

<pre><code>  create  config/router.es6
  create  config/application.js
</code></pre>

<p><code>router.es6</code> would be the main entry point for our Ember App, it&rsquo;s
something similar to <code>routes.rb</code>, it will help us defined how users
interact with our application. We will talk more about it later but if
you can&rsquo;t wait, check the awesome <a href="http://emberjs.com/guides/routing/">routing
guide</a> on Ember.js.</p>

<p><code>application.js</code> is the replacement for <code>app/assets/javascript/application.js</code>.</p>

<p>Remove your <code>app/assets/javascripts</code> since it will be ignore by EAKR <code>rm -rf app/assets/javascripts</code>.</p>

<p>Start your rails server <code>rails s</code> and go to <code>http://localhost:3000</code>, you will see <code>"Welcome to Ember!"</code>.</p>

<p>We are ready to start! In the next post we will create our first
model, talk a bit about testing and keep our journey of mastery of
Ember.js with Ruby on Rails.</p>

<h3>Can&rsquo;t wait?</h3>

<ol>
<li><p>Create an &lsquo;Ember&rsquo; application template</p>

<pre><code>$ echo  &gt; app/templates/application.hbs
</code></pre></li>
<li><p>In <code>config/routes.rb</code> uncomment the last line <code>get '*foo', :to =&gt; 'landing#index'</code>. It should look like <a href="https://github.com/abuiles/ember-rails-week-1/blob/master/config/routes.rb#L64">https://github.com/abuiles/ember-rails-week-1/blob/master/config/routes.rb#L64</a></p></li>
<li>In route.es6 uncomment   <code>location: 'history'</code> it should look like <a href="https://github.com/abuiles/ember-rails-week-1/blob/master/config/router.es6#L8">https://github.com/abuiles/ember-rails-week-1/blob/master/config/router.es6#L8</a></li>
</ol>


<p>EAKR also has a scaffold command try the following</p>

<pre><code>   $ rails g scaffold clients first_name:string last_name:string --ember
   $ rake db:migrate
</code></pre>

<p>Now navigate to <code>http://localhost:3000/clients</code> &ndash; Congrats! you have your first Ember app running.</p>

<h4>Don&rsquo;t fall behind subscribe to the Ember.js with Ruby on Rails list and stay updated.</h4>

<p><form action="http://emberenos.us7.list-manage.com/subscribe/post?u=103dd05cb2005f7b5485df96d&amp;id=25604cee1d" method="post" id="mc-embedded-subscribe-form" name="mc-embedded-subscribe-form" class="validate" target="_blank" novalidate><label for="mce-EMAIL">Subscribe.</label><input type="email" value="" name="EMAIL" class="email" id="mce-EMAIL" placeholder="email address" required><!-- real people should not fill this in and expect good things - do not remove this or risk form bot signups--> <div style="position: absolute; left: -5000px;"><input type="text" name="b_103dd05cb2005f7b5485df96d_25604cee1d" value=""></div><input type="submit" value="Subscribe" name="subscribe" id="mc-embedded-subscribe" class="button"></div></form></p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Getting Started With Ember.js and Ruby on Rails: Ember Appkit Rails]]></title>
    <link href="http://blog.abuiles.com/blog/2014/01/31/getting-started-with-ember-and-ruby-on-rails-using-ember-appkit-rails/"/>
    <updated>2014-01-31T06:46:28-05:00</updated>
    <id>http://blog.abuiles.com/blog/2014/01/31/getting-started-with-ember-and-ruby-on-rails-using-ember-appkit-rails</id>
    <content type="html"><![CDATA[<p><strong>This is the second post of my series about <a href="http://blog.abuiles.com/ember-rails/">learning Ember.js
with Ruby on Rails</a>.</strong></p>

<h3>Ember App Kit</h3>

<p><a href="https://github.com/stefanpenner/ember-app-kit">Ember App Kit</a> is project started by Embereño <a href="http://twitter.com/stefanpenner">Stefan Penner</a>, from its page:</p>

<blockquote><p>Ember App Kit (EAK) is a robust starter kit for developing
applications in Ember.js. EAK makes it easy to develop, build, test,
and deploy applications independent of any back-end build process.</p></blockquote>

<p>Getting an Ember project started is really easy thanks to EAK, it
already puts the basic structure of our apps so we can focus on
what we like the most, developing.</p>

<p>But what if I want to use Ruby on Rails and Ember? How can I get all
this stuff to play nicely?.</p>

<p>There are three options:</p>

<ol>
<li>Keep two separate projects, a Rails app (probably using rails-api) and an EAK app.</li>
<li>Use the <code>ember-rails</code> which basically puts Ember&rsquo;s source files in your project and compiles your templates.</li>
<li>Use Ember Appkit Rails :).</li>
</ol>


<h3>Introducing Ember Appkit Rails.</h3>

<p><a href="https://github.com/dockyard/ember-appkit-rails">Ember Appkit Rails</a>
is a <a href="http://dockyard.com">DockYard&rsquo;s</a> project which aims to port
Ember App Kit to the Asset Pipeline.</p>

<p>Different to the <code>ember-rails</code>&rsquo;s gem, EAKR brings the same level of
importance to your Ember.js files as your Rails files (plus some other
goodies), and this has a huge impact on how we are used to look at Rails apps.</p>

<p>EAKR (Ember Appkit Rails) puts by default all your ember application
code under <code>app/</code> and configuration files under <code>config/</code>, it also
removes <code>app/assets/javascripts</code> from the assets load path and ignores
files put in that directory. All this can be configurable and
and could change for 1.0.</p>

<p>You also get some Rails generators and if you pass
<code>--ember</code>, it will create the equivalent files for Ember.</p>

<p>As an example let&rsquo;s look at the output of <code>rails g scaffold posts --ember</code></p>

<pre><code>$rails g scaffold user first_name:string last_name:string --ember
      invoke  active_record
      create    db/migrate/20140131134237_create_users.rb
      create    app/models/user.rb
      invoke    rspec
      create      spec/models/user_spec.rb
      invoke      factory_girl
      create        spec/factories/users.rb
      create  config/serializers/user_serializer.rb
      invoke  ember:resource
      create    app/models/user.es6
      create    test/models/user_test.es6
      create    app/routes/users/edit.es6
      create    app/routes/users/index.es6
      create    app/routes/users/new.es6
      create    app/routes/users/show.es6
      insert    config/router.es6
      create    app/templates/users/edit.hbs
      create    app/templates/users/index.hbs
      create    app/templates/users/new.hbs
      create    app/templates/users/show.hbs
      create    app/templates/users.hbs
      create    app/templates/users/form.hbs
      invoke  scaffold_controller
      create    app/controllers/api/v1/users_controller.rb
      invoke    rspec
</code></pre>

<p>It creates our Ruby on Rails models, controllers, Ember files and test.</p>

<p>Some important things to notice here are</p>

<ol>
<li><p>Ember.js files are located under app, e.g. <code>app/models/user.es6</code></p></li>
<li><p>Note the extension for JavaScript files: <code>.es6</code>. It means that
they are written using ECMAScript 6 syntax which then is compiled to
AMD (Hopefully GitHub will <a href="https://github.com/github/linguist/pull/910">start to highlight this file</a>)</p></li>
<li><p>Your controller is namespaced under <code>api/vX</code>, you can change the version with <code>config.ember.api_version</code>, also your controller is optimized to be a <code>json</code> end-point.</p></li>
<li><p><code>test/models/user_test.es6</code> this is actually not part of the
official version but might be integrated any time soon (I&rsquo;m using a
development branch), you will also get test for other parts of your
Ember app like Routes, Controller, and Integration.</p></li>
</ol>


<p>All this is still under heavy development and some things might change
(I&rsquo;ll keep this post updated).</p>

<p>In the next post we will start to work on an application and also get
into some of Ember.js concepts as we start to come across them.</p>

<p><strong>Subscribe to the Ember.js with Ruby on Rails list to stay updated.</strong></p>

<p><form action="http://emberenos.us7.list-manage.com/subscribe/post?u=103dd05cb2005f7b5485df96d&amp;id=25604cee1d" method="post" id="mc-embedded-subscribe-form" name="mc-embedded-subscribe-form" class="validate" target="_blank" novalidate><label for="mce-EMAIL">Subscribe.</label><input type="email" value="" name="EMAIL" class="email" id="mce-EMAIL" placeholder="email address" required><!-- real people should not fill this in and expect good things - do not remove this or risk form bot signups--> <div style="position: absolute; left: -5000px;"><input type="text" name="b_103dd05cb2005f7b5485df96d_25604cee1d" value=""></div><input type="submit" value="Subscribe" name="subscribe" id="mc-embedded-subscribe" class="button"></div></form></p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Ruby on Rails MVC Is Not the Same as Ember.js MVC]]></title>
    <link href="http://blog.abuiles.com/blog/2014/01/26/ruby-on-rails-mvc-is-not-the-same-as-ember-dot-js-mvc/"/>
    <updated>2014-01-26T23:17:52-05:00</updated>
    <id>http://blog.abuiles.com/blog/2014/01/26/ruby-on-rails-mvc-is-not-the-same-as-ember-dot-js-mvc</id>
    <content type="html"><![CDATA[<p>One of the most confusing parts for people coming from Rails (or any
other backend web application framework) it&rsquo;s that what Ember.js
calls MVC is very different to what other frameworks call MVC.</p>

<p>To understand what MVC is in both frameworks I recommend watching
Yehuda Katz presentation:
<a href="http://www.youtube.com/watch?v=s1dhXamEAKQ">A tale of two MVC&rsquo;s</a>.</p>

<iframe width="853" height="480" src="http://blog.abuiles.com//www.youtube.com/embed/s1dhXamEAKQ" frameborder="0" allowfullscreen></iframe>


<p>Instead of talking about MVC, Yehuda proposes an unified model for GUI programming which is composed by a series of steps and then explains how different frameworks handle each step.</p>

<p>The following are the steps mention by Yehuda and the components in
charge of each in both Ruby on Rails and Ember.js.</p>

<h2>Step 1: Bootstrap Object</h2>

<p>Setup the initial state, bootstrap objects that you will require in
the future. e.g. Current User, Blog Posts.</p>

<ul>
<li>Ruby on Rails: Router and Controller</li>
<li>Ember.js: Route.</li>
</ul>


<h2>2. Draw Initial UI</h2>

<p>With the objects you setup on step 1 you will draw whatever the initial
UI is for your system.</p>

<ul>
<li>Ruby on Rails: View</li>
<li>Ember.js: Template</li>
</ul>


<p>** Views in Ember are not the same as Views in Rails, the
   equivalent are the templates, you will learn later what views are for.</p>

<h2>3. Translate Raw Input into User intent</h2>

<p>Once the user start to use the app, we want to interpret their intend,
for example clicking somewhere, pressing a key.</p>

<ul>
<li>Ruby on Rails: Template/Browser</li>
<li>Ember.js: Template or View.</li>
</ul>


<h2>4. Update application state</h2>

<p>As user starts to interact with the system, we need to update the
state of the app, for example we have an option box which is always in
the head, if the user click &ldquo;hide options&rdquo;, we need to
update the state of the app to reflect that and remember during the
rest of the user&rsquo;s session.</p>

<ul>
<li>Ruby on Rails: Controller (In the Session)</li>
<li>Ember.js: Route</li>
</ul>


<h2>5. Update Domain Objects</h2>

<p>If I remove a tweet for example, then I want to remove it from the system.</p>

<ul>
<li>Ruby on Rails: Controller</li>
<li>Ember.js: Controller</li>
</ul>


<h2>6. Notify UI Changes</h2>

<p>When there is something new, the UI needs to know somehow about it,
for example, there are new comments or the value for a field changed.</p>

<ul>
<li>Ruby on Rails: HTTP.</li>
<li>Ember.js: Data Binding.</li>
</ul>


<h2>7. Update the UI.</h2>

<p>New changes or whatever needs to be drawn is drawn.</p>

<ul>
<li>Ruby on Rails: Browser</li>
<li>Ember.js: Template</li>
</ul>


<p>The next time you are struggling to decide where to put something just look at the steps above and see where it belongs.</p>

<p>P.S I&rsquo;m <a href="http://blog.abuiles.com/ember-rails/">writing a series of post on working with Ember.js and Ruby on Rails</a>, subscribe to my list and I will let you know every time I publish an article on Ember.js and Ruby on Rails.</p>

<p><form action="http://emberenos.us7.list-manage.com/subscribe/post?u=103dd05cb2005f7b5485df96d&amp;id=25604cee1d" method="post" id="mc-embedded-subscribe-form" name="mc-embedded-subscribe-form" class="validate" target="_blank" novalidate><label for="mce-EMAIL">Subscribe.</label><input type="email" value="" name="EMAIL" class="email" id="mce-EMAIL" placeholder="email address" required><!-- real people should not fill this in and expect good things - do not remove this or risk form bot signups--> <div style="position: absolute; left: -5000px;"><input type="text" name="b_103dd05cb2005f7b5485df96d_25604cee1d" value=""></div><input type="submit" value="Subscribe" name="subscribe" id="mc-embedded-subscribe" class="button"></div></form></p>
]]></content>
  </entry>
  
</feed>
