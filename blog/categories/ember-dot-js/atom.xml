<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: ember.js | Random Stuff, Ember.js mostly.]]></title>
  <link href="http://blog.abuiles.com/blog/categories/ember-dot-js/atom.xml" rel="self"/>
  <link href="http://blog.abuiles.com/"/>
  <updated>2014-12-16T10:55:53-05:00</updated>
  <id>http://blog.abuiles.com/</id>
  <author>
    <name><![CDATA[Adolfo Builes]]></name>
    <email><![CDATA[builes.adolfo@gmail.com]]></email>
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[Ember-cli Anatomy]]></title>
    <link href="http://blog.abuiles.com/blog/2014/08/29/ember-cli-anatomy/"/>
    <updated>2014-08-29T13:30:05-05:00</updated>
    <id>http://blog.abuiles.com/blog/2014/08/29/ember-cli-anatomy</id>
    <content type="html"><![CDATA[<p>Every new developer coming to Ember.js is probably using ember-cli,
but I&rsquo;ve noticed that most of the time they are not fully aware of
ember-cli&rsquo;s main components and how they interact together so I took
this from my <a href="https://leanpub.com/ember-cli-101">ember-cli-101</a> book
where I try to explain it.</p>

<p><code>ember-cli</code> is a <code>Node.js</code> command line application sitting on top of
other libraries.</p>

<p>Its main component is <a href="https://github.com/broccolijs/broccoli">Broccoli</a>, which allows us to have fast builds,
<code>Broccoli</code> is a builder designed with the goal of keeping builds as
fast as possible (currently there are some issues on Mac which slows down the build time but there is work in progress to fix it).</p>

<p>When we run <code>ember server</code>, <code>Broccoli</code> compiles our project and put it
in a directory where it can be served using
<a href="http://expressjs.com/"><code>Express.js</code></a> which is a <code>Node.js</code> library.
<code>Express</code> is not only used to served files but also to extend
<code>ember-cli</code> functionality using its <code>middlewares</code>, an example of this
is the <code>http-proxy</code> which supports the <code>--proxy</code> option, allowing us
to develop against our development backend.</p>

<p>Testing is powered by <code>QUnit</code> and <code>Testem</code>, we can always navigate to
<code>http:/localhost:4200/tests</code> and our test will be run automatically.
We can also run Testem in <code>CI</code> or <code>--development</code> mode with the <code>ember
test</code> command. Currently there is just support for <code>QUnit</code> through an
<code>ember-cli add-on</code>, we will probably see support for other testing
frameworks and runners as people get familiar with the add-on system.</p>

<p><code>ember-cli</code> uses it&rsquo;s <a href="https://github.com/stefanpenner/ember-jj-abrams-resolver">own
resolver</a>
and has a different naming convention to <code>Ember.js's</code> defaults.</p>

<p><code>ember-cli</code> makes us write our application using <code>ES6 Modules</code>, then the code gets transpiled (compiled)[1] to <code>AMD</code>[2] and finally it is loaded with <code>loader.js</code> which is a minimalist <code>AMD</code> loader.</p>

<p>If you want to use coffee script you can use it but it is encouraged
to use plain JS and ES6 where possible.</p>

<p>Finally we need to cover <code>Broccoli</code>&rsquo;s plugins because without them,
<code>Broccoli</code> wouldn&rsquo;t be as helpful. Every transformation that your
files are going through, are done with a <code>Broccoli</code> plugin, e.g.
transpiling, minifying, finger-printing, uglifying. You can have your
own <code>Broccoli</code> plugins and plug it wherever you want in the build
process.</p>

<p><strong> <a href="https://leanpub.com/ember-cli-101">Learn how to write apps with Ember.js and ember-cli</a> with my <a href="https://leanpub.com/ember-cli-101">ember-cli-101 book</a>.</strong></p>

<ul>
<li>[1]: The transpiring process is done with <a href="https://github.com/esnext/es6-module-transpiler">es6-module-transpiler</a>.</li>
<li>[2]: To know more about <code>AMD</code> checkout <a href="https://github.com/amdjs/amdjs-api/wiki/AMD">their wiki</a></li>
</ul>

]]></content>
  </entry>
  
</feed>
