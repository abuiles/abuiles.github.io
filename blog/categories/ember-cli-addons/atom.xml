<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: Ember-cli-addons | Random Stuff, Ember.js mostly.]]></title>
  <link href="http://blog.abuiles.com/blog/categories/ember-cli-addons/atom.xml" rel="self"/>
  <link href="http://blog.abuiles.com/"/>
  <updated>2016-02-24T13:25:02-05:00</updated>
  <id>http://blog.abuiles.com/</id>
  <author>
    <name><![CDATA[Adolfo Builes]]></name>
    <email><![CDATA[builes.adolfo@gmail.com]]></email>
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[Removing Prototype Extensions With Ember-watson]]></title>
    <link href="http://blog.abuiles.com/blog/2015/03/30/removing-prototype-extensions-with-ember-watson/"/>
    <updated>2015-03-30T10:34:20-05:00</updated>
    <id>http://blog.abuiles.com/blog/2015/03/30/removing-prototype-extensions-with-ember-watson</id>
    <content type="html"><![CDATA[<p>If you follow Ember folks on twitter you might have noticed that a
bunch of them keep talking about something called prototype extensions
and disabling them. What&rsquo;s that? without getting into much details is
what allow us to set observers or computed properties calling
<code>.observes</code> or <code>property</code> at the end of a function.</p>

<p>Recently a pull request [1] was open by Stefan Penner to encourage the
use of decorator styles for declaring observers and computed
properties, in means that instead of declaring our computed properties
and observers like:</p>

<p><div>
  <pre><code class='JavaScript'>fullName: function() {
  // make fullName
}.property(&amp;lsquo;model.firstName&amp;rsquo;, &amp;lsquo;model.lastName&amp;rsquo;),
doSomething: function() {
  // do something
}.observes(&amp;lsquo;model.firstName&amp;rsquo;, &amp;lsquo;model.lastName&amp;rsquo;)</code></pre>
</div>
</p>

<p>We&rsquo;ll use the following syntax</p>

<p><div>
  <pre><code class='JavaScript'>fullName: Ember.computed(&amp;lsquo;model.firstName&amp;rsquo;, &amp;lsquo;model.lastName&amp;rsquo;, function() {
  // make fullName
}),
doSomething: Ember.observer(&amp;lsquo;model.firstName&amp;rsquo;, &amp;lsquo;model.lastName&amp;rsquo;, function() {
  // do something
})</code></pre>
</div>
</p>

<p>If you are like &ldquo;OMG! that Stefan guy is crazy! I have like 1000
computed properties and observers&rdquo; then no worries, good tool
<a href="https://github.com/abuiles/ember-watson">ember-watson</a> got you
covered.</p>

<p>Thanks to <a href="twitter.com/kamal">@kamal</a> we can update our code
automatically running the following commands:</p>

<p><div>
  <pre><code class='bash'>npm install ember-watson@latest &amp;mdash;save-dev
ember watson:convert-prototype-extensions</code></pre>
</div>
</p>

<p><strong>Also, if you have 1000 observers then think you have $10000 in debt
(more about that later).</strong></p>

<p><strong><a href="https://leanpub.com/ember-cli-101">Do you want to learn Ember with a private tutor? Get my living book Ember CLI 101 and keep your Ember knowledge up to date</a></strong></p>

<p>[1] &ndash; <a href="https://github.com/emberjs/guides/pull/110">https://github.com/emberjs/guides/pull/110</a></p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Upgrading Your Ember CLI QUnit Tests in Less Than 5 Minutes]]></title>
    <link href="http://blog.abuiles.com/blog/2015/02/25/upgrading-your-ember-cli-qunit-tests-in-less-than-5-minutes/"/>
    <updated>2015-02-25T08:56:31-05:00</updated>
    <id>http://blog.abuiles.com/blog/2015/02/25/upgrading-your-ember-cli-qunit-tests-in-less-than-5-minutes</id>
    <content type="html"><![CDATA[<p>Recently ember CLI updated its tests blueprints to match QUnit&rsquo;s 2.0
upcoming syntax. Meaning that if we wanted to live on the edge we would need to update our existing tests to match such changes or ignore the changes to <a href="https://github.com/ember-cli/ember-cli/pull/3197/files#diff-26f52f3fec353435f47247381c4f1189L12">tests/.jshintrc</a> and continue using globals.</p>

<p>As an early adopter I decided to upgrade my test, the first time I
went old school with <code>perl</code> and <code>xargs</code> running commands like:</p>

<p><code>
find  . -type f | xargs perl -pi -e "s/ deepEqual\(/assert\.deepEqual(/g"
find . -type f | xargs perl -pi -e "s/'\, function\(\)/', function\(assert\)/g"a
</code></p>

<p>The command above worked fine on the first try but it was mostly
because I run it on a personal project and I knew that nothing else
would match something like <code>'\, function\(\)/</code> when referring to
<code>test('foo', function() { ..</code>.</p>

<p>Part of my consulting work is helping my client&rsquo;s teams stay in the flow
while also living on the edge, if there is an ember CLI release, they
wanted it. If there is an Ember or Ember-Data release, they want it.</p>

<p>So naturally they also wanted to upgrade to ember-cli 0.2.0-beta.1,
but the regular expressions were not working properly for all scenarios.</p>

<p>Instead of wasting time making my regex work for every different
situation I decided to take a different approach and write an addon to
do the work for me without relying on regular expressions but,
manipulating the syntax tree directly with
<a href="https://github.com/benjamn/recast">recast&rsquo;s</a> help.</p>

<p>The basic idea is that once the addon is installed I could then do
something like <code>ember watson:upgrade-qunit-tests</code> and get the job done.</p>

<h2>Enter ember-watson</h2>

<p><a href="https://github.com/abuiles/ember-watson">ember-watson</a> is a doctor tool distributed as an addon or CLI which you
can use to help you fix different parts of your code.</p>

<p>Right now it only includes the command <code>upgrade-qunit-tests</code> which
will fix your QUnit tests to have the right syntax.</p>

<p>To use with <code>ember CLI</code> run <code>npm install ember-watson@latest --save-dev</code> and
then you can do <code>ember watson:upgrade-qunit-tests</code> getting all your tests
fixed.</p>

<p>If you are not using <code>ember CLI</code> you can use it too, check the instruction <a href="https://github.com/abuiles/ember-watson#using-without-ember-cli">here</a>.</p>

<h2>What&rsquo;s next for ember-watson?</h2>

<p>Next I&rsquo;d like to add support to <a href="blog/2015/02/09/update-your-finds/">update Ember Data
finds</a> so we don&rsquo;t have to worry
about changes on the API, I think this one will be a bit more trickier
but not impossible, if you want to contribute you can find the repo in
<a href="https://github.com/abuiles/ember-watson">https://github.com/abuiles/ember-watson</a>.</p>

<p><strong><a href="https://leanpub.com/ember-cli-101">Did you find this useful? Learn Ember and support my work at the same time buying my living book Ember CLI 101</a>.</strong></p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Know Your Addons: Ember-cli-build-notifications]]></title>
    <link href="http://blog.abuiles.com/blog/2015/02/12/know-your-addons-ember-cli-build-notifications/"/>
    <updated>2015-02-12T11:29:12-05:00</updated>
    <id>http://blog.abuiles.com/blog/2015/02/12/know-your-addons-ember-cli-build-notifications</id>
    <content type="html"><![CDATA[<p>As today <a href="http://www.emberaddons.com">emberaddons.com</a> registers 602
(and growing) ember-cli addons distributed via NPM, this means that
addons and ember-cli are starting to become the de-facto tools to
share and build  application code.</p>

<p>Addons allows us to do a bunch of different things and it seems to me
that people are not fully aware of all the scenarios where they can be
used. If your answer to any of the following question is yes, then use an addon.</p>

<br/>


<ul>
<li>Do you want to write a component that can be used in thousands of different apps?</li>
<li>Do you want to plug in the build system and do something cool with the result?</li>
<li>Do you want to extend the generators?</li>
<li>Do you want to share parts of your code between different apps?</li>
</ul>


<br/>


<p>If you know that I wrote a book about Ember (and ember-cli), you might
be thinking: &ldquo;ok, now you are going to tell me that if I want to
learn all this then I should buy your book&rdquo; No! I&rsquo;m not <a href="#learn-ember">*</a>!</p>

<p>This post is actually to introduce a new experiment I want to start
called &ldquo;Know your addons&rdquo;, I&rsquo;ll be covering different addons that I
find useful and go a bit into their implementation and mention things
that you could find worthwhile.</p>

<h2>ember-cli-build-notifications</h2>

<p>I&rsquo;d like to start with
<a href="https://www.npmjs.com/package/ember-cli-build-notifications">ember-cli-build-notifications</a>
an useful addon to display notifications when there is a build error.</p>

<p><img src="https://s3.amazonaws.com/f.cl.ly/items/3m0843433l1F0J0m1M3p/example.png" alt="ember-cli-build-notifications" /></p>

<h3>How?</h3>

<p>Philip Dudley is the creator of this addon and the code can be found in <a href="https://github.com/pdud/ember-cli-build-notifications">GitHub</a>.</p>

<p>The important file to look at for this addon is the <a href="https://github.com/pdud/ember-cli-build-notifications/blob/master/index.js">index.js</a> which is the main entry point for the addon.</p>

<p><div>
  <pre><code class='javascript'>/&lt;em&gt; jshint node: true &lt;/em&gt;/
&amp;lsquo;use strict&amp;rsquo;;&lt;/p&gt;

&lt;p&gt;var notifier = require(&amp;lsquo;./lib/notify&amp;rsquo;);&lt;/p&gt;

&lt;p&gt;module.exports = {
  name: &amp;lsquo;ember-cli-build-notifications&amp;rsquo;,&lt;/p&gt;

&lt;p&gt;  buildError: function(error) {&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;notifier.notify({
  title: &#39;Build Failed&#39;,
  subtitle: error.file,
  message: error.toString()
});
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;  }
};</code></pre>
</div>
</p>

<p>When installed in your ember-cli project&rsquo;s <code>package.json</code>, it will be
identified as add addon because of the keyword <code>"ember-addon"</code> Once it
loads, Philip is using the <a href="https://github.com/ember-cli/ember-cli/blob/6c66d2f28ade2e3c58974c353498048a8a55ef4a/ADDON_HOOKS.md#builderror">buildError
hook</a>
which gets called every time the build errors and then as soon as it
gets called it uses his notify implementation to show us the message. Clever right?</p>

<p>So the next time your boss forces you to notify them every time that crazy ember-cli build fails, you can write your own addon which will use the <code>buildError</code> to automate the process:</p>

<p><div>
  <pre><code class='javascript'>/&lt;em&gt; jshint node: true &lt;/em&gt;/
&amp;lsquo;use strict&amp;rsquo;;&lt;/p&gt;

&lt;p&gt;var bossNotifier = require(&amp;lsquo;./lib/notifier&amp;rsquo;);&lt;/p&gt;

&lt;p&gt;module.exports = {
  name: &amp;lsquo;boss-notifier&amp;rsquo;,&lt;/p&gt;

&lt;p&gt;  buildError: function(error) {&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;bossNotifier.notify({
  title: &#39;The build failed, it was not my fault&#39;,
  subtitle: error.file,
  message: error.toString()
});
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;  }
};</code></pre>
</div>
</p>

<p><strong><a name="learn-ember"></a>Actually you should :), Learn Ember with my book, check it out <a href="https://leanpub.com/ember-cli-101">leanpub.com/ember-cli-101</a> or <a href="/ember-rails">subscribe to my list for the free webinars</a></strong></p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Ruby on Rails MVC Is Not the Same as Ember.js MVC]]></title>
    <link href="http://blog.abuiles.com/blog/2014/01/26/ruby-on-rails-mvc-is-not-the-same-as-ember-dot-js-mvc/"/>
    <updated>2014-01-26T23:17:52-05:00</updated>
    <id>http://blog.abuiles.com/blog/2014/01/26/ruby-on-rails-mvc-is-not-the-same-as-ember-dot-js-mvc</id>
    <content type="html"><![CDATA[<p>One of the most confusing parts for people coming from Rails (or any
other backend web application framework) it&rsquo;s that what Ember.js
calls MVC is very different to what other frameworks call MVC.</p>

<p>To understand what MVC is in both frameworks I recommend watching
Yehuda Katz presentation:
<a href="http://www.youtube.com/watch?v=s1dhXamEAKQ">A tale of two MVC&rsquo;s</a>.</p>

<iframe width="853" height="480" src="http://blog.abuiles.com//www.youtube.com/embed/s1dhXamEAKQ" frameborder="0" allowfullscreen></iframe>


<p>Instead of talking about MVC, Yehuda proposes an unified model for GUI programming which is composed by a series of steps and then explains how different frameworks handle each step.</p>

<p>The following are the steps mention by Yehuda and the components in
charge of each in both Ruby on Rails and Ember.js.</p>

<h2>Step 1: Bootstrap Object</h2>

<p>Setup the initial state, bootstrap objects that you will require in
the future. e.g. Current User, Blog Posts.</p>

<ul>
<li>Ruby on Rails: Router and Controller</li>
<li>Ember.js: Route.</li>
</ul>


<h2>2. Draw Initial UI</h2>

<p>With the objects you setup on step 1 you will draw whatever the initial
UI is for your system.</p>

<ul>
<li>Ruby on Rails: View</li>
<li>Ember.js: Template</li>
</ul>


<p>** Views in Ember are not the same as Views in Rails, the
   equivalent are the templates, you will learn later what views are for.</p>

<h2>3. Translate Raw Input into User intent</h2>

<p>Once the user start to use the app, we want to interpret their intend,
for example clicking somewhere, pressing a key.</p>

<ul>
<li>Ruby on Rails: Template/Browser</li>
<li>Ember.js: Template or View.</li>
</ul>


<h2>4. Update application state</h2>

<p>As user starts to interact with the system, we need to update the
state of the app, for example we have an option box which is always in
the head, if the user click &ldquo;hide options&rdquo;, we need to
update the state of the app to reflect that and remember during the
rest of the user&rsquo;s session.</p>

<ul>
<li>Ruby on Rails: Controller (In the Session)</li>
<li>Ember.js: Route</li>
</ul>


<h2>5. Update Domain Objects</h2>

<p>If I remove a tweet for example, then I want to remove it from the system.</p>

<ul>
<li>Ruby on Rails: Controller</li>
<li>Ember.js: Controller</li>
</ul>


<h2>6. Notify UI Changes</h2>

<p>When there is something new, the UI needs to know somehow about it,
for example, there are new comments or the value for a field changed.</p>

<ul>
<li>Ruby on Rails: HTTP.</li>
<li>Ember.js: Data Binding.</li>
</ul>


<h2>7. Update the UI.</h2>

<p>New changes or whatever needs to be drawn is drawn.</p>

<ul>
<li>Ruby on Rails: Browser</li>
<li>Ember.js: Template</li>
</ul>


<p>The next time you are struggling to decide where to put something just look at the steps above and see where it belongs.</p>

<p>P.S I&rsquo;m <a href="http://blog.abuiles.com/ember-rails/">writing a series of post on working with Ember.js and Ruby on Rails</a>, subscribe to my list and I will let you know every time I publish an article on Ember.js and Ruby on Rails.</p>

<p><form action="http://emberenos.us7.list-manage.com/subscribe/post?u=103dd05cb2005f7b5485df96d&amp;id=25604cee1d" method="post" id="mc-embedded-subscribe-form" name="mc-embedded-subscribe-form" class="validate" target="_blank" novalidate><label for="mce-EMAIL">Subscribe.</label><input type="email" value="" name="EMAIL" class="email" id="mce-EMAIL" placeholder="email address" required><!-- real people should not fill this in and expect good things - do not remove this or risk form bot signups--> <div style="position: absolute; left: -5000px;"><input type="text" name="b_103dd05cb2005f7b5485df96d_25604cee1d" value=""></div><input type="submit" value="Subscribe" name="subscribe" id="mc-embedded-subscribe" class="button"></div></form></p>
]]></content>
  </entry>
  
</feed>
