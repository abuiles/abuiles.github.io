<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: Ember-cli, | Random Stuff, Ember.js mostly.]]></title>
  <link href="http://blog.abuiles.com/blog/categories/ember-cli/atom.xml" rel="self"/>
  <link href="http://blog.abuiles.com/"/>
  <updated>2015-02-25T10:18:20-05:00</updated>
  <id>http://blog.abuiles.com/</id>
  <author>
    <name><![CDATA[Adolfo Builes]]></name>
    <email><![CDATA[builes.adolfo@gmail.com]]></email>
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[Lightning Fast Deployments With Rails (in the Wild).]]></title>
    <link href="http://blog.abuiles.com/blog/2014/07/08/lightning-fast-deployments-with-rails/"/>
    <updated>2014-07-08T16:30:18-05:00</updated>
    <id>http://blog.abuiles.com/blog/2014/07/08/lightning-fast-deployments-with-rails</id>
    <content type="html"><![CDATA[<p><strong><a href="https://leanpub.com/ember-cli-101">Getting started with Ember.js? I cover different deploying strategies in my Ember.js book</a></strong></p>

<p>A couple of months ago I wrote an article about deploying ember-cli
apps to heroku. Looking back, it was more a dirty hack to get things
working than a long term solution.</p>

<p>During Ember Conf  I had the opportunity to talk with some of the @yapplabs folks
about the method they were using to deploy their apps and
then Luke Melia gave an interesting talk on Rails Conf [1] about it.</p>

<p>Then, there was a blog post by Feifan Wang [2] implementing Luke&rsquo;s concepts, and I used
that as the backbone for my current solution.</p>

<p>Instead of prefixing the current deploy with timestamps, we put all
the files on the same dir to avoid cache busting. By default when
accessing the app in Rails, we&rsquo;ll serve serve the latest stable
release which is stored in Redis as &ldquo;release:index.html&rdquo;, or if the
user asks for the &ldquo;canary&rdquo; version (?version=canary) then serve the
latest known deployment.</p>

<p>The reason for including a &lsquo;canary&rsquo; release [3] is that I can test stuff
in production before making it available to the public, also being
able to serve other versions, means we can develop different features
and share the same &ldquo;staging&rdquo; app, and then just test by passing as
param the feature&rsquo;s shortSHA.</p>

<p>There are more advantages about this approach, check out Luke&rsquo;s video
for more enlightment!</p>

<h1>Generating the build.</h1>

<p>First let&rsquo;s examine the Brocfile, we just tell Broccoli to fingerprint our assets preprending our cloudfront url to all of them.
the final result would look something like <code>https://d29bb5msqib8gy.cloudfront.net/assets/vendor-6673dc1e2f6a7bece01002d43fae1b5b.js</code>.</p>

<p>```
var EmberApp = require(&lsquo;ember-cli/lib/broccoli/ember-app&rsquo;);</p>

<p>var app = new EmberApp({
  // broccoli-asset-rev is now an ember-cli addon.
  fingerprint: {</p>

<pre><code>prepend: 'https://d29bb5msqib8gy.cloudfront.net/'
</code></pre>

<p>  },
  minifyCSS: {</p>

<pre><code>enabled: true,
options: {}
</code></pre>

<p>  }
});</p>

<p>module.exports = app.toTree();
```</p>

<p>With that we get our desired &lsquo;dist&rsquo; output. Next we need to upload it
to S3 (we might be able to do this directly with ember-cli soon)</p>

<h2>Gruntfile</h2>

<p>Next we tell grunt to upload to S3 and push the generated index to
Redis. The default task publish the index as a canary release (meaning
it doesn&rsquo;t get served by default) just if the user explicitly requires
<code>?version=canary</code>. If we want to make the current version the
one that the users get served then we run <code>grunt
publish-release</code>.</p>

<p>Normally we&rsquo;ll run the default task on our CI server against staging,
and then when things get merged into production run the release task,
optionally we can run always the default task against production, in that way your
clients can try the latest version of the app before making it
available to everyone else.</p>

<p>```
module.exports = function(grunt) {
  grunt.initConfig({</p>

<pre><code>env: grunt.file.readJSON('.env'),
s3: {
  options: {
    key: '&lt;%= env.AWS_ACCESS_KEY_ID %&gt;',
    secret: '&lt;%= env.AWS_SECRET_ACCESS_KEY %&gt;',
    bucket: '&lt;%= env.AWS_BUCKET %&gt;',
    access: 'public-read',
    headers: {
      "Cache-Control": "max-age=630720000, public",
      "Expires": new Date(Date.now() + 630720000).toUTCString()
    },
  },
  dev: {
    upload: [
      {
        src: 'dist/assets/**/*',
        dest: 'assets/',
        rel: 'dist/assets',
        options: { verify: true }
      }
    ]
  }
},
redis: {
  options: {
    manifestKey: 'releases',
    manifestSize: 10,
    host: '&lt;%= env.REDISTOGO.host %&gt;',
    port: '&lt;%= env.REDISTOGO.port %&gt;',
    connectionOptions: {
      auth_pass: '&lt;%= env.REDISTOGO.password %&gt;'
    }
  },
  canary: {
    options: {
      prefix: '&lt;%= gitinfo.local.branch.current.shortSHA %&gt;:',
      currentDeployKey: '&lt;%= gitinfo.local.branch.current.shortSHA %&gt;',
    },
    files: {
      src: ["dist/index.html"]
    }
  },
  release: {
    options: {
      prefix: 'release:'
    },
    files: {
      src: ["dist/index.html"]
    }
  }
},
</code></pre>

<p>  });
  grunt.loadNpmTasks(&lsquo;grunt-gitinfo&rsquo;);
  grunt.loadNpmTasks(&lsquo;grunt-s3&rsquo;);
  grunt.loadNpmTasks(&lsquo;grunt-redis&rsquo;);</p>

<p>  grunt.registerTask(&lsquo;release&rsquo;, [&lsquo;gitinfo&rsquo;, &lsquo;redis:release&rsquo;]);
  grunt.registerTask(&lsquo;canary&rsquo;, [&lsquo;gitinfo&rsquo;, &lsquo;redis:canary&rsquo;]);
  grunt.registerTask(&lsquo;publish-release&rsquo;, [&lsquo;default&rsquo;, &lsquo;release&rsquo;]);
  return grunt.registerTask(&lsquo;default&rsquo;, [&lsquo;gitinfo&rsquo;, &lsquo;s3:dev&rsquo;, &lsquo;canary&rsquo;]);
};
```</p>

<h1>Rails</h1>

<p>In rails we will need to have <code>redis</code> gem installed and then
in our root action just serve the index.html from Redis.</p>

<p>```
class LandingController &lt; ApplicationController
  def index</p>

<pre><code>render text: index_html
</code></pre>

<p>  end</p>

<p>  private</p>

<p>  def index_html</p>

<pre><code>redis.get "#{deploy_key}:index.html"
</code></pre>

<p>  end</p>

<p>  # By default serve release, if canary is specified then the latest
  # known release, otherwise the requested version.
  def deploy_key</p>

<pre><code>params[:version] ||= 'release'
case params[:version]
when 'release' then 'release'
when 'canary'  then  redis.lindex('releases', 0)
else
  params[:version]
end
</code></pre>

<p>  end</p>

<p>  def redis</p>

<pre><code>if Rails.env.development?
  redis = Redis.new()
else
  Redis.new(:url =&gt; ENV['REDISTOGO_URL'])
end
</code></pre>

<p>  end
end
```</p>

<p>And voilá! You have separated your Ember.js and Rails app with super fast deployments!</p>

<p>Dealing with CSRF can be done injecting the csrf token in your header
and then telling Ember to pick it up for you, or use my
<a href="https://github.com/abuiles/rails-csrf">rails-csrf</a> plugin which will
take care of everything, it just requires and end-point for
fetching the csrf token.</p>

<p>If you want to see this in a real app check the client-side part in
<a href="https://github.com/abuiles/facturas-client/blob/master/Gruntfile.js">facturas-client</a>
and the Rails part in <a href="https://github.com/abuiles/facturas/commit/391fcfad81ba8afcabf33bd349d05ba19e3073da">facturas Rails</a></p>

<h2>Standing on the shoulder of giants.</h2>

<p>Thanks Luke, for your Rails Conf talk!</p>

<p><img src="http://cl.ly/image/1Z3M2B1Q2X2a/Screen%20Shot%202014-07-08%20at%202.12.20%20PM.png" alt="Luke Melia" /></p>

<p>Thanks Feifan Wang for you blog post and <code>grunt-redis-manifest</code>.</p>

<h4>Pair with me.</h4>

<p>If you are looking for help with Ember.js/ember-cli/Rails, I do a free hour of
pairing every week, shoot me an email to <a href="&#109;&#x61;&#x69;&#108;&#116;&#x6f;&#x3a;&#98;&#117;&#105;&#x6c;&#101;&#115;&#46;&#97;&#100;&#111;&#108;&#x66;&#x6f;&#64;&#103;&#109;&#97;&#105;&#x6c;&#46;&#99;&#x6f;&#109;">&#98;&#x75;&#x69;&#x6c;&#x65;&#115;&#x2e;&#x61;&#100;&#x6f;&#x6c;&#102;&#x6f;&#x40;&#103;&#x6d;&#x61;&#105;&#x6c;&#46;&#99;&#x6f;&#109;</a>.</p>

<p><strong><a href="https://leanpub.com/ember-cli-101">Get my Ember.js book and to learn how to write apps with Ember.js, Ruby on Rails and ember-cli</a></strong></p>

<ul>
<li>[1] <a href="https://www.youtube.com/watch?v=QZVYP3cPcWQ">RailsConf 2014 &ndash; Lightning Fast Deployment of Your Rails-backed JavaScript app</a></li>
<li>[2] <a href="https://medium.com/@feifanw/framework-agnostic-fast-zero-downtime-javascript-app-deployment-df40cf105622">Framework agnostic, fast zero-downtime Javascript app deployment</a></li>
<li>[3] <a href="http://martinfowler.com/bliki/CanaryRelease.html">Canary Release</a></li>
</ul>

]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Deploying Ember-cli and Rails to Heroku]]></title>
    <link href="http://blog.abuiles.com/blog/2014/05/21/deploying-ember-cli-and-rails-to-heroku/"/>
    <updated>2014-05-21T08:09:55-05:00</updated>
    <id>http://blog.abuiles.com/blog/2014/05/21/deploying-ember-cli-and-rails-to-heroku</id>
    <content type="html"><![CDATA[<p><strong> this post is out-dated use <a href="http://blog.abuiles.com/blog/2014/07/08/lightning-fast-deployments-with-rails">Lightning Fast Deployments instead</a> </strong></p>

<p>I moved the app which I was doing as part of my Ember.js with Rails
series to ember-cli, but just until very recently deployed to heroku.</p>

<p>I want to put my ember-cli output into my <code>rails/public</code> and then
include the scripts into my application layout</p>

<p>```html
&lt;!DOCTYPE html>
<html>
  <head></p>

<pre><code>&lt;base href="http://blog.abuiles.com/" /&gt;

&lt;link rel="stylesheet" href="ember-assets/vendor.css"&gt;
&lt;link rel="stylesheet" href="ember-assets/client.css"&gt;
&lt;%= csrf_meta_tags %&gt;
</code></pre>

<p>  </head>
  <body></p>

<pre><code>&lt;%= yield %&gt;
&lt;script&gt;
  window.ENV = {"railsCsrf":{"csrfURL":"api/v1/csrf"},"baseURL":"/","locationType":"auto","FEATURES":{"query-params-new":true},"APP":{}};
&lt;/script&gt;
&lt;script src="ember-assets/client.js"&gt;&lt;/script&gt;
&lt;script&gt;
  window.Client = require('client/app')['default'].create(ENV.APP);
&lt;/script&gt;
</code></pre>

<p>  </body>
</html>
```</p>

<p>I do it like this because I want to include my <code>csrf_tags</code> in the body
and also have the option of preloading any data if I need to.</p>

<p>Out of topic, but since handling csrf-tags is such a common scenario I
wrote <a href="http://github.com/abuiles/rails-csrf">rails-csrf</a>, a plugin
which you can install through bower for handling CSRF for you.</p>

<h2>Building</h2>

<p>I came through a script by Jason Madsen(@jason_madsen) which would
take care of building the ember-cli app and putting it under public,
it works great but the final output is under your <code>public/index.html</code>
which I don&rsquo;t want.</p>

<p>I modified the script to do a couple of things more.</p>

<ol>
<li>Add <code>&lt;%= csrf_meta_tags %&gt;</code> before head (line 40).</li>
<li>Add <code>&lt;%= yield %&gt;</code> after body (line 43).</li>
<li>Replace <code>application.html.erb</code> with my final <code>index.html</code> (line 46)</li>
</ol>


<p>The script can be found on the following <a href="https://gist.github.com/abuiles/5c281123fc41e6b988e3">gist</a></p>

<h2>Deploying</h2>

<p>Now with the desired output, is just a matter of <code>git commit</code> and <code>git push</code> to heroku.</p>

<p>To make it easier I added the following task to my Rakefile, it:</p>

<ol>
<li>Accepts the &ldquo;env&rdquo; I want to deploy and the remote name (heroku end-point).</li>
<li>Moves to the specified env and merges master.</li>
<li>Runs the <code>build</code> script and commit the changes.</li>
<li>Push the changes to heroku.</li>
</ol>


<p>```ruby
task :deploy, :env, :remote do |t, args|
  sh &ldquo;git checkout #{args[:env]}&rdquo;
  sh &lsquo;git merge master -m &ldquo;Merging for deployment&rdquo;&rsquo;
  sh &lsquo;./build.sh&rsquo;</p>

<p>  sh &lsquo;git add -A&rsquo;
  sh &lsquo;git commit -m &ldquo;Compile for deployment&rdquo;&rsquo;</p>

<p>  sh &ldquo;git push #{args[:remote]} head:master&rdquo;
end
```</p>

<p>A standard deployment would be something like <code>rake deploy[production, heroku]</code></p>

<p>It assumes you have a branch called <code>production</code> and that your remote is called <code>heroku</code>.</p>

<h2>Acknowledgements</h2>

<p>Thanks to Jason Madsen for his initial script, he also made a rails template <a href="https://github.com/knomedia/ember-cli-rails">knomedia/ember-cli-rails</a>.</p>

<p>The rake task was mostly inspired by <a href="https://github.com/dockyard/ember-cli-plus-backend">DockYard&rsquo;s ember-cli-plus-backend</a>.</p>

<h2>Future work</h2>

<p>There is still a lot of things to improve which I&rsquo;ll be blogging about as part of my ember-cli + Rails series. Here&rsquo;s a list of things:</p>

<ol>
<li>Deploying to heroku just the Rails files, currently we include the ember-cli app as well.</li>
<li>Using cloud front for serving your assets.</li>
<li><del>Fingerprinting</del> is super simple with <a href="https://github.com/rickharrison/broccoli-asset-rev">broccoli-asset-rev</a> see it in use <a href="https://github.com/abuiles/facturas/commit/12b23d5fc36d0906a1e21cae7aed97bdbb8cba9e">here</a>.</li>
<li>Doing lightning fast deployments (see <a href="http://www.confreaks.com/videos/3324-railsconf-lightning-fast-deployment-of-your-rails-backed-javascript-app">Luke&rsquo;s Melia talk on Rails Conf</a>).</li>
</ol>


<p><strong><a href="http://blog.abuiles.com/ember-rails/">Learn how to write apps with Ember.js, ember-cli and Ruby on Rails</a>, subscribe to my list and I will let you know every time I publish an article. </strong></p>
]]></content>
  </entry>
  
</feed>
